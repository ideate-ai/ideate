# WI-176: Update db.ts row in architecture.md source code index

## Context

The source code index in `architecture.md` has a row for `mcp/artifact-server/src/db.ts`. WI-173 updated five other module rows but left the `db.ts` row incomplete — it omits two key exports added in cycle 016–019 (Q-64):

- `metricsEvents` — exported at `db.ts:171` (Drizzle table for `metrics_events`)
- `TYPE_TO_DRIZZLE_TABLE` — exported at `db.ts:235` (mapping from string type to Drizzle table)

## File to Modify

`specs/plan/architecture.md`, around line 577.

## Current State

```
| mcp/artifact-server/src/db.ts | TypeScript | workItems, findings, domainPolicies, domainDecisions, domainQuestions, guidingPrinciples, constraints, moduleSpecs, researchFindings, journalEntries, edges, nodeFileRefs, documentArtifacts, AnyTable |
```

## What to Change

Add `metricsEvents` and `TYPE_TO_DRIZZLE_TABLE` to the key exports column. The updated row should be:

```
| mcp/artifact-server/src/db.ts | TypeScript | workItems, findings, domainPolicies, domainDecisions, domainQuestions, guidingPrinciples, constraints, moduleSpecs, researchFindings, journalEntries, edges, nodeFileRefs, documentArtifacts, metricsEvents, AnyTable, TYPE_TO_DRIZZLE_TABLE |
```

Place `metricsEvents` after `documentArtifacts` (it's the next table defined in the file) and `TYPE_TO_DRIZZLE_TABLE` after `AnyTable` (it's defined after the `AnyTable` union type).

## Verification

1. Read `architecture.md` line 577 — confirm both exports are present
2. No other rows changed
