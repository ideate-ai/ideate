# WI-159: Spec cleanup — stale notes, criteria, scope entries

## Context

Spec-reviewer M2, M3 from cycle 016. Minor documentation drift between spec notes and implementation.

## Implementation

### plan/notes/148.md

In the edge type table, add `work_item` to the source types for `belongs_to_domain`. The current table lists only `domain_policy`, `domain_decision`, `domain_question`.

Also update the table to reflect `addressed_by` and `amended_by` (reversed from `addresses` and `amends`) per D-59.

### plan/work-items.yaml

1. WI-149 scope entry: change `{path: scripts/migrate-to-v3.test.ts, op: create}` to `{path: mcp/artifact-server/src/__tests__/migrate.test.ts, op: create}`

2. WI-144 criteria: update the `idx_edges_composite` criterion to: `'SQLite schema has a UNIQUE constraint on (source_id, target_id, edge_type) in the edges table, providing an equivalent implicit composite index'`
