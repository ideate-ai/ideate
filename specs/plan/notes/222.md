# WI-222: Resolve Open Domain Questions Post-v3

## Q-20 Resolution

Current status: open
New status: resolved

Resolution: The v3 architecture stores findings under `.ideate/cycles/{NNN}/findings/`. Both brrr and standalone review write to the same location. The review manifest location divergence described in Q-20 no longer applies — both paths now produce findings in the cycle directory structure.

## Q-77 Resolution

Current status: open
New status: resolved

Resolution: Resolved by WI-221 which updates Section 9 of architecture.md to list all tools/ directory files (tools/index.ts, tools/context.ts, tools/query.ts, tools/execution.ts, tools/analysis.ts, tools/write.ts).

## Format for resolution notes

Each question gets a resolution note appended:
