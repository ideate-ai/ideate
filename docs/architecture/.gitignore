# Generated by render.sh — rebuilt from the markdown beside it. The PDF is
# deliberately NOT ignored: it is the artifact handed to the alignment session.
build/

# The three phantom-store paths that used to be ignored here are gone: the
# cause closed on 2026-08-07. `loadConfig` no longer onboards wherever it is
# pointed — the command-line tools resolve the project root by walking upward
# for an enclosing `.ideate.json` (src/config/ideate-config.ts's
# `findProjectRoot`, guarded by src/config/project-root.test.ts), so a call
# whose working directory is this one joins the project's store instead of
# starting a new one beside it. Board item 01KZ72R4NBMH9CDD0CDHS7FMDA.
