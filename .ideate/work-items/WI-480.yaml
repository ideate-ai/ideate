id: WI-480
type: work_item
title: "handleBootstrapWorkspace: skip config overwrite if already initialized"
status: done
complexity: small
scope:
  - path: mcp/artifact-server/src/tools/bootstrap.ts
    op: modify
depends: []
blocks: []
criteria:
  - If config.json already exists in the target directory, handleBootstrapWorkspace does not overwrite it
  - New directories are still created (mkdirSync recursive is idempotent)
  - npm run test passes
domain: artifact-structure
phase: null
notes: |-
  ## Context
  handleBootstrapWorkspace calls createIdeateDir unconditionally, which overwrites config.json. The dormant path (handleBootstrapDormant) already checks for existing config.

  ## Implementation
  Check readIdeateConfig(projectRoot) first. If config exists, skip createIdeateDir and just ensure subdirectories exist.
resolution: null
cycle_created: 6
cycle_modified: 5
content_hash: 8ccddc4099869e21e187aeee5e707d04f7bfe9e7bcb80185a7b6723929352bd7
token_count: 220
