id: WI-602
type: work_item
title: "Align write path: putNode, patchNode, batchMutate, nextId, archiveCycle, appendJournalEntry"
status: pending
complexity: large
scope:
  - path: mcp/artifact-server/src/adapters/remote/index.ts
    op: modify
  - path: ideate-server/src/resolvers/mutations/node.ts
    op: modify
  - path: ideate-server/src/resolvers/mutations/batch.ts
    op: modify
  - path: ideate-server/src/resolvers/mutations/lifecycle.ts
    op: modify
depends:
  - WI-600
blocks: []
criteria:
  - putNode + getNode round-trip returns equivalent properties from both adapters
  - patchNode + getNode returns equivalent node from both adapters
  - batchMutate returns equivalent results arrays from both adapters
  - nextId returns same IDs from both adapters for finding and journal_entry
  - archiveCycle succeeds consistently on both adapters
  - appendJournalEntry creates entry with matching IDs from both adapters
  - T-13 putNode event_name=null test passes
  - All 11 write/batch/lifecycle equivalence tests pass
domain: agent-system
phase: PH-025
work_item_type: bug
notes: "Server putNode must store content = JSON.stringify(properties). RemoteAdapter mapGqlNodeToNode must handle properties for nodes written via putNode (not just migration). batchMutate result format alignment. nextId strategy alignment (max+1 vs count). archiveCycle message format. appendJournalEntry ID format. T-13: decide whether indexer stops coercing null or RemoteAdapter applies same fallback."
resolution: null
cycle_created: null
cycle_modified: null
content_hash: 5b7b136fa136d1a037349075c060c48be2bf0e12315495f4a19bfcd6c64927fe
token_count: 387
