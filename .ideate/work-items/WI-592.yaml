id: WI-592
type: work_item
title: Equivalence tests — node CRUD and edge operations
status: done
complexity: medium
scope:
  - path: mcp/artifact-server/src/__tests__/equivalence-crud.test.ts
    op: create
depends:
  - WI-591
blocks: []
criteria:
  - getNode returns identical Node for every fixture artifact ID (all ~20 artifacts tested)
  - getNodes returns identical Map for a batch of 5+ IDs
  - readNodeContent returns non-empty string for every fixture artifact from both adapters
  - getEdges(id, 'outgoing') returns identical edge sets for a node with known outgoing edges
  - getEdges(id, 'incoming') returns identical edge sets for a node with known incoming edges
  - getEdges(id, 'both') returns identical combined edge sets
  - All 6 schema v5 extension columns are present and equal in node properties
  - putNode + getNode round-trip produces equivalent results on both adapters
  - patchNode + getNode produces equivalent results on both adapters
  - deleteNode + getNode produces null from both adapters
domain: agent-system
phase: PH-024
work_item_type: feature
notes: For read-only tests, query the pre-loaded fixture data. For write tests (putNode, patchNode, deleteNode), use a separate describe block that writes test-specific data to avoid polluting the fixture state. The readNodeContent comparison may need normalization (YAML serialization vs JSON serialization) — compare parsed objects rather than raw strings if they differ.
resolution: null
cycle_created: null
cycle_modified: 5
content_hash: 91008b0722c0336857bf0e12d4bae3fb37da15839164d9a5ce780abc8b1234d6
token_count: 401
