id: WI-491
type: work_item
title: Add handleWriteArtifact dispatch branches for metrics_event and interview_question
status: done
complexity: low
scope:
  - path: mcp/artifact-server/src/tools/write.ts
    op: modify
  - path: mcp/artifact-server/src/__tests__/tools.test.ts
    op: modify
depends:
  - WI-489
blocks: []
criteria:
  - handleWriteArtifact has explicit dispatch branches for metrics_event and interview_question that call the appropriate upsert functions
  - Writing a metrics_event via handleWriteArtifact populates both nodes and metrics_events extension table
  - Writing an interview_question via handleWriteArtifact populates both nodes and interview_questions extension table
  - Integration tests verify both types round-trip correctly
  - Existing tests pass without regression
domain: artifact-structure
phase: null
notes: |-
  ## What to change

  ### write.ts
  In handleWriteArtifact's Phase 2 type dispatch, add branches for metrics_event and interview_question that call the appropriate upsert functions from db-helpers.ts.

  ### tools.test.ts
  Add two tests: write each type via handleWriteArtifact, then query the extension table to confirm data is present.

  ## Why
  Q-112: These types pass P-42 validation but have no dispatch branch, causing silent extension data loss.
resolution: null
cycle_created: null
cycle_modified: 5
content_hash: f1bd9b624f2d1d2b7e0f13da355bec4ecda4419c2e65c85c888fa78fb25d9bfa
token_count: 340
