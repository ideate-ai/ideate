id: WI-699
type: work_item
title: Add dispatchHook allowlist rejection test (P-58 compliance)
complexity: low
scope:
  - path: mcp/artifact-server/src/__tests__/hooks.test.ts
    op: modify
depends: []
blocks: []
criteria:
  - A test exists that calls dispatchHook with a non-allowlisted command (e.g. 'rm', 'curl', or 'python').
  - The test asserts the call throws an error whose message matches the allowlist rejection message defined at src/hooks.ts:206-214.
  - The test is co-located with the existing hook tests in src/__tests__/hooks.test.ts.
  - Existing hook tests continue to pass. Test suite passes with zero regressions.
domain: artifact-structure
phase: null
work_item_type: bug
notes: |-
  The allowlist is at src/hooks.ts:87-95: ALLOWLISTED_COMMANDS = new Set(['git', 'node', 'npm', 'npx', 'echo', 'cat', 'ls']). The rejection is at lines 206-214: `throw new Error(\`Command "${parsed.command}" is not allowlisted...\`)`.

  All existing command-type tests use allowlisted commands (echo, node). Add one test:
  ```
  it('dispatchHook rejects non-allowlisted command', async () => {
    const hook = { on: 'work_item.completed', command: 'rm -rf /' };
    await expect(dispatchHook(hook, {})).rejects.toThrow(/not allowlisted/);
  });
  ```
  Adjust the hook format to match what the actual dispatchHook signature expects. Read the existing tests to understand the correct call format.
resolution: null
status: done
cycle_created: 21
cycle_modified: 19
content_hash: 61f6571910e9c091974469e448e8ad34623278b07ccf1885496d50e57bda0094
token_count: 391
