id: WI-503
type: work_item
title: Fix settings skill worker agent inconsistency
status: done
complexity: low
scope:
  - path: skills/settings/SKILL.md
    op: modify
depends: []
blocks: []
criteria:
  - Either worker is added to the Phase 2a agent table (making it 10 agents) OR worker is removed from the Phase 0 defaults list
  - The Phase 0 defaults count and Phase 2a table row count are consistent
  - No other settings content is modified
domain: workflow
phase: null
notes: |-
  ## What to change

  The settings skill Phase 0 lists worker (maxTurns=200, model=sonnet) as a default agent, but Phase 2a says '9 agents' and the table has no worker row. worker is not in DEFAULT_AGENT_BUDGETS in config.ts.

  Decision: Remove worker from the Phase 0 defaults list since it has no backing default in the config layer. The worker agent's budget is controlled by the execute skill directly (it uses the Agent tool's default behavior), not through config.json agent_budgets.
resolution: null
cycle_created: null
cycle_modified: 5
content_hash: b0bc564a2273266ececc8af69b543dae5729a8fe3a44ab59b14d995670641a9e
token_count: 257
