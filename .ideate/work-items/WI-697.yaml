id: WI-697
type: work_item
title: Tighten alpha and max_iterations boundary validation in LocalContextAdapter
complexity: small
scope:
  - path: mcp/artifact-server/src/adapters/local/context.ts
    op: modify
  - path: mcp/artifact-server/src/__tests__/ppr.test.ts
    op: modify
depends: []
blocks: []
criteria:
  - "traverse({alpha: 0}) throws ValidationError. Verified by test."
  - "traverse({max_iterations: 0}) throws ValidationError. Verified by test."
  - "Error messages for alpha say 'must be > 0' (not '>= 0') and 'valid range: (0, 1]'."
  - Error messages for max_iterations say 'must be > 0' (not '>= 0').
  - Existing tests for alpha=-0.1 and max_iterations=-1 remain passing.
  - No changes to ppr.ts required — only context.ts validation tightened.
  - Test suite passes with zero regressions.
domain: adapter-equivalence
phase: null
work_item_type: bug
notes: |-
  In context.ts at lines 231-232, change:
    `alpha < 0 || alpha > 1` → `alpha <= 0 || alpha > 1`
    `max_iterations < 0` → `max_iterations <= 0`

  Update the error message strings to reflect the new bounds (e.g., 'valid range: (0, 1]' instead of '[0, 1]', 'must be > 0' instead of '>= 0').

  The matching validation in RemoteAdapter is handled by WI-698. Both fixes must produce identical error codes so adapter-equivalence tests continue to pass.
resolution: null
status: done
cycle_created: 21
cycle_modified: 19
content_hash: 780e67db5540047afa843ddccb65adbfdf88bb43ff55dcf53f7e5b1ee7a09091
token_count: 374
