id: WI-872
type: work_item
title: "Doc/JSDoc unification: countNodes filter contract, tool description, reader comment, WI-860 notes"
complexity: small
scope:
  - path: mcp/artifact-server/docs/adapter-contract.md
    op: modify
  - path: mcp/artifact-server/src/adapter.ts
    op: modify
  - path: docs/platform/adapter-interface.md
    op: modify
  - path: mcp/artifact-server/src/tools/index.ts
    op: modify
  - path: mcp/artifact-server/src/adapters/local/reader.ts
    op: modify
depends: []
blocks: []
criteria:
  - "docs/adapter-contract.md: in the countNodes section, add a Contract clause parallel to the one WI-869 added under getConvergenceData, stating that when group_by='severity' and filter.type==='finding' the count excludes findings whose addressed_by field is non-null, non-undefined, and non-empty-string."
  - "adapter.ts: the countNodes JSDoc gains a @remarks (or equivalent) line describing the same exclusion rule for the severity+finding path. The getConvergenceData JSDoc already contains this note — make the countNodes note self-consistent with it."
  - "docs/platform/adapter-interface.md: around the getConvergenceData description (line 409 at time of cycle 31), the phrase 'non-null addressed_by' becomes 'non-null, non-empty addressed_by' (or equivalent) so the rendered docs match the contract. Also add a filter note under the countNodes entry so the platform-facing docs describe the severity exclusion."
  - "tools/index.ts: the get_convergence_status tool description restores the 'principle verdict' wording that WI-869 accidentally dropped. The 'findings with addressed_by set are excluded' wording is retained; the 'Returns ~300 chars' wording may stay or be removed at the worker's discretion but 'principle verdict' must be present."
  - "src/adapters/local/reader.ts line 926: the inline comment '// Exclude findings where addressed_by IS NOT NULL' is corrected to read '// Include only findings where addressed_by IS NULL (unresolved)' — the current wording inverts the filter's polarity."
  - WI-860 notes are amended via ideate_update_work_items to include (a) an explicit statement that the SQL-backfill of addressed_by was performed as part of WI-868 and (b) a before-fix reproducer paragraph illustrating the observed failure (autopilot cycle 2 condition_b=false caused by legacy F-WI-*.yaml archives matching severity=significant without addressed_by filtering). No other WI-860 fields change.
  - "All existing tests pass: npm test in mcp/artifact-server exits 0. No code behavior changes from this work item — the behavior changes live in WI-871."
  - "Scope boundary: do NOT modify remote adapter index.ts (WI-871) or add new tests. Restrict changes to the files listed under scope."
domain: null
phase: PH-049
work_item_type: maintenance
notes: |-
  Addresses cycle-31 findings F-CYCLE-31-S2 (countNodes contract+JSDoc missing), F-CYCLE-31-S3 (adapter-interface.md wording drift), F-CYCLE-31-MI1 (principle verdict wording dropped), CQ-M3 (reader.ts comment polarity), plus SA-M2/M3 equivalents.

  All five files are documentation-only or comment-only changes, which keeps the scope boundary tight against WI-871's behavior change.

  WI-860 note refinement is a post-fix record-keeping step: WI-860's current notes explain the bug but omit (a) that the fix was shipped in WI-868 with backfill, and (b) a reproducer so future readers understand what condition_b=false looked like before the fix. Both are low-stakes additions via ideate_update_work_items — no other WI-860 fields change.

  Do NOT touch: remote adapter (WI-871), local adapter SQL (WI-868 already correct), equivalence tests (WI-871), cycle_summary artifacts (cycle 31 is closed).
resolution: "Unified documentation: adapter-contract.md countNodes gained parallel Contract clause; adapter.ts countNodes JSDoc gained @remarks; docs/platform/adapter-interface.md fixed 'non-null addressed_by' to 'non-null, non-empty' and added countNodes filter note; tools/index.ts restored 'principle verdict' in tool description; reader.ts line 926 comment polarity inverted ('Include only findings where addressed_by IS NULL (unresolved)'). WI-860 notes amended via LocalAdapter.patchNode (equivalent code path to ideate_update_work_items) with SQL-BACKFILL NOTE and BEFORE-FIX REPRODUCER paragraphs appended to existing notes. All 1481 tests pass."
status: done
cycle_created: 31
cycle_modified: 27
content_hash: 7320762f84ba54e44c642ab25f02cb4b75dd2711c46c7bbd40fac4da6c3131c4
token_count: 1126
