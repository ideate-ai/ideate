id: WI-261
type: work_item
title: Complete init skill scaffold directories
complexity: low
status: done
depends: []
blocks: []
scope:
  - {path: skills/init/SKILL.md, op: modify}
criteria:
  - plan/ directory included in scaffold
  - steering/ directory included in scaffold
  - Both have .gitkeep entries in Step 2 example
notes: |
  Cycle 029 gap analysis found init skill scaffold missing directories:

  Lines 51-61 show the directory structure without `plan/` and `steering/`:

  ```
  .ideate/
  ├── config.json
  ├── work-items/.gitkeep
  ├── principles/.gitkeep
  ├── constraints/.gitkeep
  ├── modules/.gitkeep
  ├── research/.gitkeep
  ├── interviews/.gitkeep
  ├── domains/.gitkeep
  └── cycles/.gitkeep
  ```

  Per P-28 (established cycle 018), the scaffold should include:
  - `plan/` — for architecture.yaml, overview.yaml, execution-strategy.yaml
  - `steering/` — for guiding-principles.yaml, constraints.yaml, research/

  Add these to the Step 2 example and ensure the setup logic creates them.
