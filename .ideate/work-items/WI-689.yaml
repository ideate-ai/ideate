id: WI-689
type: work_item
title: Fix adapter-contract.md patchNode missing IMMUTABLE_FIELD error code; fix stale comment in local-adapter-validation.test.ts
complexity: low
scope:
  - path: docs/adapter-contract.md
    op: modify
  - path: tests/adapters/local-adapter-validation.test.ts
    op: modify
depends: []
blocks: []
criteria:
  - adapter-contract.md patchNode Error Codes section includes IMMUTABLE_FIELD_VIOLATION (or the actual code produced when an immutable field change is attempted), matching the putNode section's documentation
  - Verify what code the LocalAdapter patchNode actually produces when an immutable field is changed — use the actual code, not an assumed one
  - The stale comment in local-adapter-validation.test.ts AC-7 describe block is corrected to accurately describe the block's content
  - No test logic is changed — only the comment string
domain: adapter-system
phase: null
work_item_type: chore
notes: |-
  MG3 from cycle 23 gap analysis (patchNode doc omission) and MG1 (stale comment). Two small fixes that can be done together since they touch different files with no logic impact.

  For the patchNode fix: grep for IMMUTABLE in src/adapters/local/writer.ts and src/adapters/remote/index.ts to find the exact error code produced when `id` or `type` are present in UpdateNodeInput. The field may be named differently than putNode's IMMUTABLE_FIELD_VIOLATION. Add the correct code to the patchNode Error Codes block in adapter-contract.md.

  For the stale comment: local-adapter-validation.test.ts AC-7 describe block contains a comment that references generic 'removes edges' language. Update to accurately describe the AC-7 scope (deleteNode, putEdge, removeEdges validation).
resolution: null
status: done
cycle_created: 23
cycle_modified: 19
content_hash: c11533fc50b96e52c855dbbccac16dc8ed69eaa03e0074ac97434e9e48e45163
token_count: 471
