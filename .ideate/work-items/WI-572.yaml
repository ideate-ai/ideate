id: WI-572
type: work_item
title: Add appendJournalEntry contract test and fix execution.ts adapter stubs
status: done
complexity: small
scope:
  - path: mcp/artifact-server/src/__tests__/adapter-contract.test.ts
    op: modify
  - path: mcp/artifact-server/src/tools/execution.ts
    op: modify
depends: []
blocks: []
criteria:
  - "Contract test added for appendJournalEntry: verifies it returns a J-NNN-NNN format ID, is idempotent, correctly sequences entries within a cycle"
  - "execution.ts handleGetExecutionStatus: remove misleading adapter check and console.warn, use direct DB path only (document as intentional until adapter has aggregate methods)"
  - "execution.ts handleGetReviewManifest: same treatment"
  - All tests pass
domain: artifact-structure
phase: PH-019
work_item_type: maintenance
notes: |-
  Resolves: appendJournalEntry no contract test (II1/MI2), execution.ts misleading adapter stubs (S1/EC1/EC2).

  For contract test: add a describe block for appendJournalEntry in the contract test factory. Test: call with valid args, verify returned ID matches J-NNN-NNN pattern. Call twice with same body, verify idempotent or sequential IDs. Verify cycle scoping.

  For execution.ts: remove the if(ctx.adapter) checks that warn and fall through. These handlers don't have adapter methods yet. The honest code is direct DB access with a TODO comment referencing the future adapter method addition. No console.warn noise in production.
resolution: null
cycle_created: 11
cycle_modified: 5
content_hash: 6b8f53319f746039bec43600bc0b395150ca99b9916b8ec0081cf686e7198b2a
token_count: 401
