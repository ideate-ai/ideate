id: WI-590
type: work_item
title: Synthetic .ideate/ test fixture with known values
status: done
complexity: medium
scope:
  - path: mcp/artifact-server/src/__tests__/fixtures/equivalence/
    op: create
  - path: mcp/artifact-server/src/__tests__/fixtures/equivalence/FIXTURE-README.md
    op: create
depends: []
blocks: []
criteria:
  - Fixture directory at mcp/artifact-server/src/__tests__/fixtures/equivalence/.ideate/ contains valid YAML artifacts
  - "At least one artifact per NodeType that has extension table columns: work_item, finding, domain_policy, domain_decision, domain_question, guiding_principle, constraint, module_spec, project, phase, metrics_event, journal_entry"
  - Work item fixtures include depends, scope, domain, resolution, and criteria fields to exercise edge extraction
  - Finding fixtures include title field (schema v5 column)
  - Domain decision fixtures include title and source fields (schema v5 columns)
  - Phase fixture includes completed_date field and project fixture includes current_phase_id field
  - At least one artifact per type has nullable fields explicitly set to null
  - At least one metrics event has both event_name and agent_type set to verify precedence
  - "Fixture includes known edges: depends_on, belongs_to_phase, belongs_to_project, governed_by, derived_from"
  - All artifact IDs, field values, and edge targets are deterministic and documented in a FIXTURE-README.md
domain: artifact-structure
phase: PH-024
work_item_type: feature
notes: "Keep the fixture small (~20-25 artifacts) but comprehensive. Every extension table column from schema v5 must be exercised. Include the edge cases from the triage report: nullable cycle_created/cycle_modified, GP with title vs name, phase with project_id vs project. Use a config.json with schema_version 5. The fixture should be importable by both the LocalAdapter (YAML to SQLite indexer) and the migration CLI (YAML to Neo4j)."
resolution: null
cycle_created: null
cycle_modified: 5
content_hash: 4e71c17f4affae35d161aa8f9a043ab37d648c6ccf86a9ecb0059852e994859b
token_count: 522
