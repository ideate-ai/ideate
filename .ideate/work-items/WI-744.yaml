id: WI-744
type: work_item
title: Fix stale handler names in architecture doc file registry
status: done
complexity: low
scope:
  - path: .ideate/plan/architecture.yaml
    op: modify
    note: via MCP ideate_write_artifact only — no direct file edit
depends: []
blocks: []
criteria:
  - Architecture doc file registry lists `handleGetArtifactContext` (not `handleGetWorkItemContext`) for context.ts
  - Architecture doc file registry lists `handleGetWorkspaceStatus` (not `handleGetProjectStatus`) for analysis.ts
  - All other architecture doc content is unchanged
  - Correction made via ideate_write_artifact MCP call — no direct .ideate/ file edit
domain: artifact-structure
phase: null
notes: |-
  ## Trigger
  Cycle 20 gap-analysis MI1: two stale handler names remain in the architecture doc file registry after WI-740's targeted updates.

  ## What to change

  Call `ideate_artifact_query({type: 'architecture'})` to retrieve the current architecture doc content. Then call `ideate_write_artifact` with `type: 'architecture'` to write the corrected version.

  Two substitutions required:
  1. `handleGetWorkItemContext` → `handleGetArtifactContext`
     - This is the exported function name in `mcp/artifact-server/src/tools/context.ts:216`
     - The legacy delegator `handleGetWorkItemContext` at context.ts:597 is internal-only, not a public export
  2. `handleGetProjectStatus` → `handleGetWorkspaceStatus`
     - This is the exported function name in `mcp/artifact-server/src/tools/analysis.ts:650`
     - No function named `handleGetProjectStatus` exists

  Verify against source before writing:
  - `grep -n 'handleGetArtifactContext\|handleGetWorkItemContext' mcp/artifact-server/src/tools/context.ts`
  - `grep -n 'handleGetWorkspaceStatus\|handleGetProjectStatus' mcp/artifact-server/src/tools/analysis.ts`

  Do NOT edit .ideate/ files directly. Use MCP only.
resolution: "Updated architecture.yaml via ideate_write_artifact: handleGetWorkItemContext → handleGetArtifactContext (context.ts file registry), handleGetProjectStatus → handleGetWorkspaceStatus (analysis.ts file registry). Verified against source. No direct .ideate/ file edits."
cycle_created: 20
cycle_modified: 19
content_hash: 03080f3e0e6a819fa6ecc0a9ea01dc4f93eaaf2268e73a35469ecaecdc41075a
token_count: 550
