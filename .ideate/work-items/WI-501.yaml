id: WI-501
type: work_item
title: Add direct unit test for computeArtifactHash
status: done
complexity: low
scope:
  - path: mcp/artifact-server/src/__tests__/indexer.test.ts
    op: modify
depends: []
blocks: []
criteria:
  - A test creates an object with content_hash, token_count, and file_path fields plus regular fields
  - The test computes computeArtifactHash on the full object and on a copy with metadata fields removed
  - The test asserts both hashes are equal — proving the exclusion works
  - The test does NOT use computeArtifactHash as both the oracle and the subject — it independently constructs the expected hash
domain: artifact-structure
phase: null
notes: |-
  ## What to add

  In indexer.test.ts (which already imports computeArtifactHash), add a describe block:

  ```typescript
  describe('computeArtifactHash — direct unit test', () => {
    it('excludes content_hash, token_count, and file_path from hash computation', () => {
      const obj = { id: 'WI-001', title: 'Test', content_hash: 'stale', token_count: 99, file_path: '/fake/path' };
      const clean = { id: 'WI-001', title: 'Test' };
      // Independently compute expected hash
      const expected = createHash('sha256').update(stringify(clean, { lineWidth: 0 })).digest('hex');
      expect(computeArtifactHash(obj)).toBe(expected);
    });
  });
  ```

  Import createHash from 'crypto' and stringify from 'yaml' if not already imported.
resolution: null
cycle_created: null
cycle_modified: 5
content_hash: d34ddd37585fabcdc9ef19d14cd6649ea9500408ac317614800e22a0a9d382be
token_count: 371
