id: WI-450
type: work_item
title: Fix handleGetWorkspaceStatus to use nodes.status + fix ARCHITECTURE.md residuals + update P-41
status: done
complexity: small
scope:
  - path: mcp/artifact-server/src/tools/analysis.ts
    op: modify
  - path: ARCHITECTURE.md
    op: modify
  - path: skills/autopilot/SKILL.md
    op: modify
  - path: skills/refine/SKILL.md
    op: modify
depends:
  - WI-449
blocks: []
criteria:
  - handleGetWorkspaceStatus active project query uses n.status = 'active' (not p.status)
  - handleGetWorkspaceStatus active phase query uses n.status = 'active' (not p.status)
  - ARCHITECTURE.md projects table appetite column documented as INTEGER with numeric description
  - ARCHITECTURE.md schema version documented as 2 (not 1)
  - ARCHITECTURE.md work_items table includes phase column
  - Self-check boxes in skills/autopilot/SKILL.md changed from [ ] to [x]
  - Self-check boxes in skills/refine/SKILL.md changed from [ ] to [x]
  - P-41 policy updated via ideate_write_artifact to include workspace_label, phases_completed, and current_project
  - npm run build succeeds
domain: artifact-structure
notes: |-
  Grouped documentation and small code fixes:

  1. analysis.ts: Change both active project and active phase queries from WHERE p.status = 'active' to WHERE n.status = 'active' (join already exists to nodes table).

  2. ARCHITECTURE.md: Fix appetite column from TEXT to INTEGER with description 'Number of phases budgeted'. Fix schema version from 1 to 2. Add phase to work_items extension table column list.

  3. Self-check boxes: Mark [ ] as [x] in autopilot/SKILL.md and refine/SKILL.md for the ideate_get_project_status removal assertion.

  4. P-41: Update via ideate_write_artifact({type: 'domain_policy', id: 'P-41', content: ...}) to add workspace_label, phases_completed, and current_project to the static properties whitelist.
resolution: null
cycle_created: null
cycle_modified: 5
content_hash: dd81f690255a9885759a6b1280a47523dc1456354ad4af5078312124e9e1a7dd
token_count: 482
