id: WI-758
type: work_item
title: Update migrations.test.ts deferral comment to reference WI-758
complexity: low
scope:
  - path: mcp/artifact-server/src/__tests__/migrations.test.ts
    op: modify
depends: []
blocks: []
criteria:
  - The comment block at `migrations.test.ts:302-308` references `WI-758` as the tracking work item for the deferred no-registry-entry path
  - The comment accurately states that the deferral reason is ESM module-level constant isolation (per D-195) — `CONFIG_SCHEMA_VERSION` cannot be overridden via `vi.spyOn` in ESM mode
  - No other changes to `migrations.test.ts`
  - All tests in `migrations.test.ts` continue to pass
domain: artifact-structure
phase: PH-017
work_item_type: chore
notes: |-
  ## Implementation

  Update the comment block at lines 302-308 in `src/__tests__/migrations.test.ts`.

  **Current comment**:
  ```typescript
  // Known untested path: no-registry-entry silent bump
  // When CONFIG_SCHEMA_VERSION is bumped without a corresponding MIGRATIONS entry,
  // runPendingMigrations reaches the branch at migrations.ts:~170 which increments
  // schema_version silently without running a migration function.
  // Testing this path requires mocking CONFIG_SCHEMA_VERSION to a value beyond
  // the highest MIGRATIONS.toVersion — deferred to a future work item.
  ```

  **Replace with**:
  ```typescript
  // Known untested path: no-registry-entry silent bump (tracked as WI-758)
  // When CONFIG_SCHEMA_VERSION is bumped without a corresponding MIGRATIONS entry,
  // runPendingMigrations reaches the branch at migrations.ts:~170 which increments
  // schema_version silently without running a migration function.
  // Testing this path requires overriding CONFIG_SCHEMA_VERSION — an ESM module-level
  // constant that vi.spyOn cannot intercept (D-195). Deferred: no test infrastructure
  // for ESM module-level constant override exists in this project yet.
  ```

  No logic changes. No other file changes.
resolution: Updated deferral comment at lines 302-308 to reference WI-758 and cite D-195 (ESM module-level constant cannot be overridden via vi.spyOn). All 7 migrations.test.ts tests pass.
status: done
cycle_created: 23
cycle_modified: 21
content_hash: 216d4b03913834838327dbf53b1c6437f40fb86074b61f5e941f573f94745295
token_count: 575
