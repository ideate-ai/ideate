id: WI-711
type: work_item
title: Document deleteNode double-failure behavior in adapter-contract.md + remove batchMutate redundant FK restore
complexity: low
scope:
  - path: mcp/artifact-server/docs/adapter-contract.md
    op: modify
  - path: mcp/artifact-server/src/adapters/local/writer.ts
    op: modify
depends: []
blocks: []
criteria:
  - "deleteNode section in adapter-contract.md has an Error Handling block: when TRANSACTION_FAILED is thrown because both the SQLite DELETE and the YAML restore fail, the error message includes both the SQLite error context and the restore error context ('SQLite transaction failed: <sqlite err>; cleanup also failed: <restore err>')"
  - batchMutate redundant `if (fkWasOn) this.db.pragma('foreign_keys = ON')` removed from catch block (~line 1344); only the finally block restores the pragma
  - All tests pass (1132+)
domain: adapter-equivalence
phase: PH-037
work_item_type: bug
notes: "IR1 from cycle 21 gap-analyst: deleteNode double-failure behavior is implemented (writer.ts:938) and tested (writer.test.ts:395) but not documented in the contract. Add Error Handling block to the deleteNode contract entry (after Error Codes, around line 136) specifying that TRANSACTION_FAILED includes both error contexts in the double-failure case. Model after any existing Error Handling block (e.g., getNode section at ~line 39-40 has an Error Handling subsection). Also bundle M1 from cycle 21 code-reviewer: batchMutate catch block at ~line 1344 calls `if (fkWasOn) this.db.pragma('foreign_keys = ON')` immediately before `throw`, then finally calls it again. The catch call is dead relative to finally. Remove just that line from the catch block."
resolution: null
status: done
cycle_created: 21
cycle_modified: 19
content_hash: ae1a49b355860ea59d944cc221e7819cdce4a856caabfb39a3b7ac7d18f25f90
token_count: 463
