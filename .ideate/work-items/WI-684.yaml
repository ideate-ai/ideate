id: WI-684
type: work_item
title: "Equivalence tests: putNode validation codes + removeEdges empty-array no-op"
complexity: low
scope:
  - path: mcp/artifact-server/tests/adapters/adapter-equivalence.test.ts
    op: modify
depends:
  - WI-683
blocks: []
criteria:
  - "Equivalence pair for putNode INVALID_NODE_TYPE: invalid type value throws INVALID_NODE_TYPE from both adapters"
  - "Equivalence pair for putNode MISSING_NODE_PROPERTIES: null/absent properties throws MISSING_NODE_PROPERTIES from both adapters"
  - "Equivalence pair for removeEdges with empty edge_types array: both adapters return without error"
  - All LocalAdapter tests use plain it(...); all RemoteAdapter tests use it.skipIf(!remoteAvailable)(...)
  - Existing tests unchanged
  - Full test suite passes
domain: adapter-equivalence
phase: null
work_item_type: maintenance
notes: |-
  MG2 and removeEdges empty-array equivalence. Depends on WI-683 (which adds the RemoteAdapter empty-array guard).

  **putNode INVALID_NODE_TYPE**: `putNode({ id: 'GP-01', type: 'not_a_type' as NodeType, properties: {} })` — expect INVALID_NODE_TYPE. NodeType is imported from adapter.ts.

  **putNode MISSING_NODE_PROPERTIES**: `putNode({ id: 'GP-01', type: 'guiding_principle', properties: null as any })` or omit properties entirely — expect MISSING_NODE_PROPERTIES. Check what the actual code validates (writer.ts ~line 536, remote/index.ts ~line 583) to use the right input.

  **removeEdges empty-array no-op**: `removeEdges('GP-01', [])` — expect resolves without error (no throw). Both adapters return early. This test can use `.resolves.toBeUndefined()` or just `await expect(...).resolves`.

  Follow D-177 pattern throughout. Update file header comment to list new coverage.
resolution: null
status: pending
cycle_created: 22
cycle_modified: null
content_hash: f6c2ce907c6f253e7223e8aa721e45a9ce0e47fdbcfb09c3f08738942b70e366
token_count: 454
