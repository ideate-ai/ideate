id: WI-875
type: work_item
title: Standardize countNodes addressed_by clause wording in adapter-contract.md:398 and adapter.ts:459
complexity: low
scope:
  - path: mcp/artifact-server/docs/adapter-contract.md
    op: modify
  - path: mcp/artifact-server/src/adapter.ts
    op: modify
depends:
  - WI-873
blocks: []
criteria:
  - adapter-contract.md:398 countNodes clause reads 'non-null, non-empty' (replacing 'non-null, non-undefined, and non-empty-string')
  - adapter.ts:459 countNodes JSDoc reads 'non-null, non-empty' (replacing 'non-null, non-undefined, and non-empty-string')
  - adapter.ts:1140 _countFindingsBySeverity JSDoc updated to explicitly mention 'non-null, non-empty' (addresses F-CYCLE-33-M1)
  - All 4 surfaces (adapter-interface.md countNodes, adapter-interface.md getConvergenceData, adapter.ts countNodes JSDoc, adapter.ts getConvergenceData JSDoc, adapter-contract.md countNodes, adapter-contract.md getConvergenceData, remote/index.ts _countFindingsBySeverity JSDoc) use the single phrase 'non-null, non-empty'
  - npm test in mcp/artifact-server passes (no tests touch JSDoc strings; expect no regressions)
  - "Scope boundary: no changes to any other file. No runtime/behavioral changes. Pure documentation standardization."
domain: null
phase: PH-049
work_item_type: maintenance
notes: |-
  Addresses F-CYCLE-33-S1 (significant, spec-reviewer), F-CYCLE-33-MI1 (minor, gap-analyst, subsumed by S1), F-CYCLE-33-M1 (minor, code-reviewer — _countFindingsBySeverity JSDoc).

  Root cause: WI-873's ADAPTER-INTERFACE criterion was partially executed — adapter-interface.md was updated but adapter-contract.md:398 and adapter.ts:459 were missed. Also WI-869 (cycle 31) introduced _countFindingsBySeverity without the JSDoc wording present in adapter.ts.

  Fix: three one-line edits. No runtime impact.

  Regression risk: zero (JSDoc strings). Run npm test for safety.
resolution: "Three one-line doc-wording edits applied: (1) adapter-contract.md:398 countNodes clause now reads 'non-null, non-empty'; (2) adapter.ts:458-460 countNodes JSDoc same; (3) remote/index.ts:1141-1143 _countFindingsBySeverity JSDoc expanded to explicitly mention empty-string exclusion with SQL rationale. Verification: grep 'non-null, non-undefined, and non-empty-string' across mcp/artifact-server/ and docs/platform/ returns zero matches. npm test: 1481 passed, 0 failed, 345 skipped. Addresses F-CYCLE-33-S1 (spec), F-CYCLE-33-MI1 (gap, subsumed), F-CYCLE-33-M1 (code). Zero runtime impact."
status: done
cycle_created: 33
cycle_modified: 27
content_hash: 21fc683a38ee695f7bc0bf2e37d4d7ad13d402e7d6a628f7edf1d39c474c22c3
token_count: 660
