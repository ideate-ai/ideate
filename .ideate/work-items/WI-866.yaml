id: WI-866
type: work_item
title: Surface --limit 0 intent in report-tool-usage.sh rather than silent 1000 default
complexity: low
scope:
  - path: scripts/report-tool-usage.sh
    op: modify
depends:
  - WI-862
blocks: []
criteria:
  - Invoking the script with `--limit 0` does NOT silently revert to 1000.
  - "Behavior is one of: (a) error with a helpful message telling the user what values are accepted, or (b) honor 0 as 'no limit' if that matches the user's intent, or (c) enforce a minimum with a warning."
  - The chosen behavior is documented in the script's --help output.
  - "Manual test: passing `--limit 0` no longer produces a 1000-row report with no warning."
domain: null
phase: PH-048
work_item_type: bug
notes: Derived from cycle 28 code-reviewer finding M6. Current behavior at lines 77-80 silently rewrites 0 → 1000, which violates least-surprise. Pick one of the three options in the criteria and document. Depends on WI-862 (both touch the same script; sequentialize to avoid worktree merge conflicts).
resolution: "report-tool-usage.sh: --limit 0 and --top 0 now exit 2 with clear message. --cycle validated separately (non-negative integer) preserving legitimate --cycle 0. --help text documents N>=1 constraint. All 5 manual-test cases pass (cycle 0 succeeds; limit 0, top 0, cycle abc, cycle -1 all error with clear messages). Review Pass after one rework round (initial Fail on --cycle 0 regression)."
status: done
cycle_created: null
cycle_modified: 27
content_hash: fe3c4a86f5cbdc2679396682db6c55181d4be0f003f1498641986c496673b812
token_count: 394
