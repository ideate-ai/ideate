id: WI-850
type: work_item
title: Delete historical metrics YAML and legacy report scripts
complexity: low
scope:
  - path: .ideate/metrics/
    op: delete
  - path: scripts/report.sh
    op: delete
  - path: scripts/report-cycle.sh
    op: delete
  - path: scripts/report-cost.sh
    op: delete
  - path: scripts/report-executive.sh
    op: delete
depends:
  - WI-843
  - WI-844
blocks: []
criteria:
  - Directory .ideate/metrics/ does not exist (or exists and is empty — acceptance requires `find .ideate/metrics -type f -name 'ME-*.yaml' | wc -l` returns 0)
  - scripts/report.sh, scripts/report-cycle.sh, scripts/report-cost.sh, scripts/report-executive.sh do not exist
  - No other file in the repo references 'scripts/report.sh' or the other three scripts (grep -rn --exclude-dir=.git 'scripts/report' returns zero matches outside of historical cycle artifacts under .ideate/cycles/)
domain: null
phase: PH-048
work_item_type: maintenance
notes: |-
  Delete the entire .ideate/metrics/ directory (contains 249 ME-*.yaml files).

  Delete the four legacy report scripts. Their replacement ships in WI-858 (per P-69 removing-and-replacing in same change — note this WI removes and WI-858 replaces; the two together satisfy the principle since they're in the same phase/PR).

  Depends on WI-843/844 (emission stripped) but does NOT depend on schema removal — the YAML files are inert data and can be deleted any time after skills stop emitting.

  Grep exclusions: historical cycle artifacts in .ideate/cycles/ may contain the script names as references inside past cycle summaries / findings — those are immutable and expected to remain.

  NOTE: user explicitly overrode the saved 'keep reporting scripts' feedback for this phase — embrace YAGNI; dead code removed, not stubbed (see interview-refine-ph048 Q4).
resolution: "Functionally complete: `.ideate/metrics/` directory removed and the four legacy report scripts (report.sh, report-cycle.sh, report-cost.sh, report-executive.sh) are absent from disk. Only scripts/report-tool-usage.sh remains (the replacement). Status field was drift from the abortive PH-048 execution; closing out here during cycle-28 follow-up refinement (2026-04-16)."
status: done
cycle_created: null
cycle_modified: 27
content_hash: 6c6bf74a5272c74e0f4e04affbee9fbb4da825ecd837472a201d80d01094ec89
token_count: 587
