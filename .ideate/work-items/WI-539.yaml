id: WI-539
type: work_item
title: Fix MCP phase view scoping and add phase filter test
status: done
complexity: small
scope:
  - path: mcp/artifact-server/src/tools/analysis.ts
    op: modify
  - path: mcp/artifact-server/src/__tests__/tools.test.ts
    op: modify
depends: []
blocks: []
criteria:
  - buildPhaseView queries active phase scoped to the active project (WHERE p.project = ? using active project ID, matching buildProjectView pattern)
  - "At least one test verifies ideate_artifact_query({type: 'work_item', filters: {phase: 'PH-NNN'}}) returns only work items matching that phase"
  - npm test passes with zero failures
domain: artifact-structure
phase: null
work_item_type: feature
notes: |-
  # WI-539

  ## EC2: buildPhaseView project scoping
  In analysis.ts buildPhaseView, add project scoping. First query the active project (same as buildProjectView does), then filter the phase query by project. If no active project, fall back to current behavior.

  ## MI1: Phase filter test
  In tools.test.ts, add a test that:
  1. Creates 2 work items with different phase values
  2. Calls handleArtifactQuery with type='work_item' and filters={phase: 'PH-001'}
  3. Asserts only the matching item is returned
resolution: null
cycle_created: null
cycle_modified: 5
content_hash: 51b54a2a2849bb5b4a6b36b469514bf608dc9a4da8c8ab7f06c4a76b2d242e79
token_count: 343
