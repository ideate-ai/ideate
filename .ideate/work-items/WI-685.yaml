id: WI-685
type: work_item
title: "Clean up ppr.test.ts: remove weak redundant test, fix duplicate describe names"
complexity: low
scope:
  - path: mcp/artifact-server/src/__tests__/ppr.test.ts
    op: modify
depends: []
blocks: []
criteria:
  - The test 'throws ValidationError with correct field and value' (~line 1647) is either removed or strengthened to use toBeInstanceOf(ValidationError) and assert .code === 'INVALID_ALPHA'
  - The two describe blocks named 'computePPR — high alpha value tests' are renamed to be distinct
  - The two describe blocks named 'computePPR — concurrent computation tests' are renamed to be distinct
  - All test assertions that previously passed still pass
  - npm test passes
domain: adapter-equivalence
phase: null
work_item_type: chore
notes: |-
  M2 and M3 from Cycle 22 code quality review.

  **M2 (weak test ~line 1647)**:
  The test 'throws ValidationError with correct field and value' uses `expect(err).toBeInstanceOf(Error)` and checks only message text. The WI-681 test at ~line 1661 covers the same code path with proper assertions (toBeInstanceOf(ValidationError) and .code === 'INVALID_ALPHA'). Either remove the weak test (preferred if it's fully redundant) or strengthen it to use ValidationError and assert .code.

  **M3 (duplicate describe names)**:
  Rename the duplicate blocks to distinguish them:
  - Second 'computePPR — high alpha value tests' (~line 1236) → 'computePPR — high alpha stability tests'
  - Second 'computePPR — concurrent computation tests' (~line 1359) → 'computePPR — sequential computation isolation tests'

  Do not change any test logic or assertions — only describe block names and the one test that needs strengthening/removal.
resolution: "All scope absorbed by validation layer work. Weak test 'throws ValidationError with correct field and value' no longer present. Duplicate describe names already resolved: second high-alpha block renamed to 'computePPR — high alpha stability tests'; concurrent block is unique. Verified in code 2026-04-07."
status: done
cycle_created: 22
cycle_modified: 19
content_hash: 871f0da83682125adf5b1b56062dcbf7db4cd01a95569e2d3986d323d2551e69
token_count: 520
