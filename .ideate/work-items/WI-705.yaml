id: WI-705
type: work_item
title: Implement FILESYSTEM_ERROR throw in deleteNode + P-53 doc fix + test
complexity: low
scope:
  - path: mcp/artifact-server/src/adapters/local/writer.ts
    op: modify
  - path: mcp/artifact-server/docs/adapter-contract.md
    op: modify
  - path: mcp/artifact-server/src/__tests__/adapters/writer.test.ts
    op: modify
depends: []
blocks:
  - WI-706
criteria:
  - When fs.unlinkSync throws a non-ENOENT error (e.g. EACCES, EPERM), deleteNode throws ValidationError with code FILESYSTEM_ERROR
  - "Test: mock fs.unlinkSync to throw ErrnoException with code='EPERM', assert rejects.toMatchObject({ code: 'FILESYSTEM_ERROR' })"
  - docs/adapter-contract.md FILESYSTEM_ERROR descriptions contain no storage-format terms ('filesystem', 'unlink') — P-53 compliant
  - All existing tests continue to pass (1131+ pass, 0 fail)
domain: null
phase: null
work_item_type: bug
notes: "Phase 1 catch in writer.ts deleteNode (around line 910) currently does `if ((e as NodeJS.ErrnoException).code !== 'ENOENT') throw e` — wrap the rethrow as `throw new ValidationError('deleteNode failed: artifact removal failed: ' + (e as Error).message, 'FILESYSTEM_ERROR', { operation: 'deleteNode', id })`. In adapter-contract.md, replace FILESYSTEM_ERROR description with 'artifact removal failed' (remove 'filesystem error during node deletion unlink'). Add test to writer.test.ts deleteNode YAML rollback describe block that uses vi.spyOn(fs, 'unlinkSync').mockImplementationOnce(() => { const e = new Error('permission denied'); (e as any).code = 'EPERM'; throw e; }) and asserts rejects.toMatchObject({ code: 'FILESYSTEM_ERROR' })."
resolution: null
status: done
cycle_created: 21
cycle_modified: 19
content_hash: c97d0746724df110ee93eed0031d5b10ac05b10bba81cb6dd89d1b9aee7c6ad1
token_count: 452
