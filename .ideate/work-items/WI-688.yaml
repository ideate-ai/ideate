id: WI-688
type: work_item
title: Unify EDGE_TYPES and ALL_EDGE_TYPES into a single canonical source
complexity: small
scope:
  - path: src/schema.ts
    op: modify
  - path: src/adapter.ts
    op: modify
depends: []
blocks: []
criteria:
  - There is exactly one definition of the edge type list in the codebase
  - The other file imports from the canonical source rather than defining its own copy
  - Both EDGE_TYPES and ALL_EDGE_TYPES export names are preserved for backwards compatibility with existing imports
  - All existing callers that import EDGE_TYPES or ALL_EDGE_TYPES continue to work without modification
  - npm test passes with 0 failures
domain: adapter-system
phase: null
work_item_type: maintenance
notes: |-
  SG2 from cycle 23 gap analysis. src/schema.ts exports EDGE_TYPES (15 values). src/adapter.ts exports ALL_EDGE_TYPES (15 values). Both are maintained independently — a divergence would produce silent validation bugs.

  Decide which file is the canonical home. schema.ts is more appropriate as the schema layer. Make adapter.ts import EDGE_TYPES from schema.ts and re-export it as ALL_EDGE_TYPES:

    import { EDGE_TYPES } from '../schema';
    export const ALL_EDGE_TYPES = EDGE_TYPES;

  Verify that batchMutate in both adapters (which use these constants since WI-683) still works correctly. Check for any other callers using the grep: `EDGE_TYPES|ALL_EDGE_TYPES`.
resolution: null
status: done
cycle_created: 23
cycle_modified: 19
content_hash: 25a40b23d9a21298dd060fac6fe015779abf5148c8571fb650d50e184b87c324
token_count: 390
