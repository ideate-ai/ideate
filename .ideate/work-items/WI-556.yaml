id: WI-556
type: work_item
title: Config extension for backend selection
status: done
complexity: small
scope:
  - path: mcp/artifact-server/src/config.ts
    op: modify
  - path: mcp/artifact-server/src/server.ts
    op: modify
depends:
  - WI-551
blocks: []
criteria:
  - config.json supports a 'backend' field with values 'local' (default) or 'remote'
  - When backend is 'remote', config accepts connection fields (url, org_id — auth fields reserved for future)
  - server.ts initializes the appropriate adapter based on config.backend
  - Default behavior unchanged — omitting backend field uses LocalAdapter
  - Config schema version bumped appropriately
domain: artifact-structure
phase: PH-019
work_item_type: feature
notes: |-
  This is the config switch that enables the remote adapter in the future. For now, only 'local' is functional. 'remote' should fail gracefully with a clear error: 'Remote backend not yet implemented. Set backend to local or omit the field.'

  The server.ts initialization sequence: read config → select adapter → initialize adapter → pass to tool handlers via ToolContext.
resolution: null
cycle_created: 6
cycle_modified: 5
content_hash: 309a90c961b7e34801a773f0814885d1b6bf6d81b155ef77c23a1bff791ea840
token_count: 314
