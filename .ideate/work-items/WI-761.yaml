id: WI-761
type: work_item
title: Assert projects.current_phase_id column in schema test
complexity: low
scope:
  - path: mcp/artifact-server/src/__tests__/schema.test.ts
    op: modify
depends:
  - WI-760
blocks: []
criteria:
  - The createSchema — projects table describe block asserts that current_phase_id is present in the projects table columns
  - vitest run --reporter=verbose exits 0
domain: artifact-structure
phase: PH-017
work_item_type: bug
notes: |-
  In schema.test.ts, find the createSchema — projects table describe block (~lines 678-712). The existing column it() test builds the expected column list. current_phase_id is the 11th column in the projects DDL (schema.ts ~lines 628-641).

  If the test uses toEqual against an array: add 'current_phase_id' to the expected array.
  If the test uses toContain per column: add one more expect(cols).toContain('current_phase_id') assertion.

  Check the current test structure before editing to use the matching pattern. The architect confirmed the column exists in the DDL but is absent from the test assertion.
resolution: Added current_phase_id to the expected columns array in the projects column it() test. Test name updated to include current_phase_id. Tests pass.
status: done
cycle_created: 22
cycle_modified: 21
content_hash: de3a3b59c61f979eb3744b4e7cc853b91e6bbc8c621cfdf987836eead087cdc2
token_count: 345
