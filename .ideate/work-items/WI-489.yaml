id: WI-489
type: work_item
title: Extend assertRequiredFields to check null
status: done
complexity: low
scope:
  - path: mcp/artifact-server/src/db-helpers.ts
    op: modify
  - path: mcp/artifact-server/src/__tests__/tools.test.ts
    op: modify
depends:
  - WI-487
blocks: []
criteria:
  - assertRequiredFields rejects both undefined and null values for required fields
  - The error message distinguishes between missing (undefined) and null values
  - A unit test passes null for a required field and asserts the guard throws
  - Existing tests pass without regression
domain: artifact-structure
phase: null
notes: |-
  ## What to change

  ### db-helpers.ts (line 527)
  Extend the check to cover null:
  ```typescript
  if (row[field] === undefined || row[field] === null) {
    const reason = row[field] === undefined ? 'missing' : 'null';
    throw new Error(`upsertExtensionRow(${tableName}): required field '${field}' is ${reason}`);
  }
  ```

  ### tools.test.ts
  Add a test that passes null for a required field and asserts the error.

  ## Why
  Q-106: Null values pass the guard but violate NOT NULL SQLite constraints.
resolution: null
cycle_created: null
cycle_modified: 5
content_hash: 1288c1deb0199735f4ba8861f0137b3de9e89540273ff91c957b271e357bd6cd
token_count: 298
