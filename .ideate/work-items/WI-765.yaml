id: WI-765
type: work_item
title: Complete partial column-structure tests for 4 extension tables
complexity: small
scope:
  - path: mcp/artifact-server/src/__tests__/schema.test.ts
    op: modify
depends:
  - WI-764
blocks: []
criteria:
  - The `findings` describe block (line 424) is expanded to enumerate all 11 columns and verify NOT NULL constraints (severity, work_item, verdict, cycle, reviewer are NOT NULL; file_refs, description, suggestion, addressed_by, title are nullable)
  - The `domain_policies` describe block (line 436) is expanded to enumerate all 7 columns and verify NOT NULL constraints (domain is NOT NULL; derived_from, established, amended, amended_by, description are nullable)
  - The `domain_questions` describe block (line 448) is expanded to enumerate all 8 columns and verify NOT NULL constraints (domain is NOT NULL; impact, source, resolution, resolved_in, description, addressed_by are nullable)
  - The `document_artifacts` describe block (line 464) is expanded to enumerate all 4 columns (id, title, cycle, content) — already partially covered, add column count assertion
  - All modified tests pass (`vitest run src/__tests__/schema.test.ts`)
domain: artifact-structure
phase: PH-017
work_item_type: chore
notes: |-
  Expand existing describe blocks. Do not delete existing `it` tests — add new ones alongside them or replace the existing single-column checks with comprehensive versions.

  The existing tests check:
  - findings: `addressed_by` column only
  - domain_policies: `amended_by` column only
  - domain_questions: `addressed_by` nullability only
  - document_artifacts: title/cycle/content nullability (nearly comprehensive for 4 cols)

  For each, add a full column enumeration test following the proxy_human_decisions pattern. Keep existing it() tests if they test something the new comprehensive test doesn't (e.g., specific nullability of a single column).
resolution: "Expanded 4 partial describe blocks: findings (1→2 tests, all 11 columns + NOT NULL), domain_policies (1→2 tests, all 7 columns + NOT NULL), domain_questions (1→2 tests, all 8 columns + NOT NULL), document_artifacts (1→2 tests, all 4 columns + NOT NULL). Total test count: 100."
status: done
cycle_created: null
cycle_modified: 21
content_hash: f267be688678ac87f47e8c4d5ea715abd8ed31cc9973a84f3343b4b36d1e4c97
token_count: 587
