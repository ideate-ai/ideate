id: WI-864
type: work_item
title: Document both-view truncation semantics for getToolUsage
complexity: low
scope:
  - path: mcp/artifact-server/src/tools/tool-usage.ts
    op: modify
  - path: mcp/artifact-server/README.md
    op: modify
depends:
  - WI-861
blocks: []
criteria:
  - A comment at tool-usage.ts (~lines 153-161) explains how aggregate and detail sections truncate under the both view — which section caps first, at what limit, and what happens when both exceed the budget.
  - mcp/artifact-server/README.md user-facing documentation mentions the both-view truncation behavior (one paragraph minimum).
  - Documented behavior matches actual behavior (verified by reading the code).
domain: null
phase: PH-048
work_item_type: maintenance
notes: Derived from cycle 28 code-reviewer finding M4. Current truncation is silent and undocumented; readers must trace the slice logic to understand what data they get back. Depends on WI-861 for context-field coherence (the doc may reference session/cycle/phase fields). No behavior change — just documentation of existing semantics.
resolution: "Comment at tool-usage.ts:161 explains aggregate computed from all rows (never truncated) vs detail capped at limit via rows.slice(0, limit); newest rows beyond limit dropped (ordering: timestamp ASC, id ASC). README.md Tool-usage-reporting section added with paragraph + example. New test exercises truncation semantics including direction check (ROW_4 retained, ROW_5 dropped) so a DESC regression would fail the test. 1471/1471 tests pass. Review Pass after one rework round (initial Fail on directional error: 'oldest' was backwards)."
status: done
cycle_created: null
cycle_modified: 27
content_hash: d42d0d22b65b54b8ce6b444cad9249a869d4f8c6391a7a1dd9ce645d957ddc1d
token_count: 447
