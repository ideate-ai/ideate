id: WI-267
type: work_item
title: Standardize README placeholder to project_root
complexity: low
status: done
depends: []
blocks: []
scope:
  - {path: mcp/artifact-server/README.md, op: modify}
criteria:
  - All documentation examples use {project_root} not {artifact_dir}
  - Consistent with D-028-1 and D-029-3 canonical placeholder
notes: |
  Full audit 20260326 found README uses deprecated placeholder:

  Lines 98-120 in example tool calls use `artifact_dir` parameter name.
  While this is the actual MCP tool parameter name (which is correct),
  the documentation should use {project_root} as the placeholder value
  for consistency with other skills and agents.

  Lines to check/update:
  - Line 18: "All tools accept `artifact_dir` as a parameter"
  - Lines 98-120: Example code blocks showing tool usage

  Note: The actual parameter name `artifact_dir` in the tool definition
  is correct and should not change. Only the placeholder in examples
  should be standardized to {project_root}.
