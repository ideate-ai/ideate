id: WI-530
type: work_item
title: Fix buildProjectView horizon and buildPhaseView status display
status: done
complexity: small
scope:
  - path: mcp/artifact-server/src/tools/analysis.ts
    op: modify
  - path: mcp/artifact-server/src/__tests__/tools.test.ts
    op: modify
depends: []
blocks: []
criteria:
  - buildProjectView reads horizon from SQLite projects table JSON column instead of regex on YAML file
  - Horizon section displays phase names by looking up phases table for each ID in horizon.next
  - "buildPhaseView SELECT includes n.status and output includes **Status**: line"
  - Test for view=project includes horizon verification
  - Test for view=phase includes dependency edges verification
  - npm test passes with zero failures
domain: artifact-structure
phase: null
work_item_type: feature
notes: "Fixes S1 (horizon display) and S6 (phase status). In analysis.ts buildProjectView: replace the readFileSafe + regex block (lines ~400-420) with a query: SELECT p.horizon FROM projects p JOIN nodes n ON n.id = p.id WHERE n.status = 'active'. Parse JSON, iterate horizon.next array of phase IDs, look up names via SELECT name FROM phases WHERE id = ?. In buildPhaseView: add n.status to the SELECT at line ~428, add **Status**: line to output. In tests: add horizon and dependency edge test data."
resolution: null
cycle_created: null
cycle_modified: 5
content_hash: 1417fb73602dce95a0b29f75aceb2a704c847ab6dc725b6b4bdc6d6babf1fd86
token_count: 366
