id: WI-883
type: work_item
title: "Fix parser multiline regex + test sweeps: /m flag + Violation all-bold + P-33 real path + provenance audit"
complexity: medium
scope:
  - path: mcp/artifact-server/src/tools/analysis.ts
    op: edit
  - path: mcp/artifact-server/src/__tests__/tools.test.ts
    op: edit
depends: []
blocks: []
criteria:
  - "Criterion 1 (S1 parser fix): analysis.ts step 2 body-pattern regexes use /m flag: /^###\\s/m and /^\\s*-\\s/m"
  - "Criterion 2 (S1 regression test a): a new test exercises a body beginning with a prose sentence followed by '### P-14' on a later line and asserts principle_verdict: fail, source: step2"
  - "Criterion 3 (S1 regression test b): a second new test uses a body starting with a prose sentence followed by '- Item A' on a later line and asserts principle_verdict: fail, source: step2"
  - "Criterion 4 (S5 Violation all-bold tests): two new tests cover '**Principle Violation Verdict: Pass**' (asserts verdict: pass, source: step1) and '**Principle Violation Verdict: Fail**' (asserts verdict: fail, source: step1)"
  - "Criterion 5 (S2 P-33 realistic path): the P-33 fixture test at tools.test.ts:1643-1680 uses fixture content containing an absolute-path reference like '/workspace/.ideate/cycles/028/spec-adherence.yaml' that PATH_LEAK_RE would match if leaked"
  - "Criterion 6 (S2 pre-check): a meta-assertion expect(fixtureContent).toMatch(PATH_LEAK_RE) is added at the top of the P-33 test, proving the regex would fire if code leaked the path. The existing expect(result).not.toMatch(PATH_LEAK_RE) still passes because the step3 warning does not contain the content snippet verbatim"
  - "Criterion 7 (S3 provenance sweep): every test in tools.test.ts that asserts principle_verdict: pass|fail|unknown also asserts principle_verdict_source: step1|step2|step3. Audited lines include at minimum: 848, 880, 905, 929, 948, 981, 1015, 1139, 1174, 1187, 1214, 1322, 1343, 1363, 1378, 1434, 1455, 1496, 4191, 4214, 4237"
  - "Criterion 8: npm test passes with 0 failures"
domain: null
phase: PH-050
work_item_type: bug
notes: |-
  Addresses 4 significant findings from cycle 28 capstone:
  - F-CYCLE-28-S1 (code-reviewer S1): Step 2 body-pattern regexes missing /m flag. /^###\s/ and /^\s*-\s/ anchor to start of joined body string; prose-first bodies cause fall-through to step 3. Fix at analysis.ts:153-157.
  - F-CYCLE-28-S2 (code-reviewer S2): P-33 test at tools.test.ts:1671-1673 vacuously passes because fixture uses relative '.ideate/' path that PATH_LEAK_RE (which requires leading /) cannot match. Fix the fixture + add meta-assertion.
  - F-CYCLE-28-S3 (spec-reviewer S1): Pre-existing parser tests (21 enumerated lines) assert principle_verdict without asserting source. WI-881 criterion 5 not met.
  - F-CYCLE-28-S5 (gap-analyst SIG-1): JSDoc claims 8 enumerated variants but Violation all-bold has no test. Add 2 tests parallel to cycle 322/323.

  All changes are in analysis.ts (1 hunk: /m flag) + tools.test.ts (multiple hunks: 2 multiline step2 tests + 2 Violation all-bold tests + P-33 fixture + provenance sweep).

  Implementation order within the worker session:
  1. Apply /m flag to analysis.ts:153-157
  2. Add 2 multiline step2 regression tests
  3. Add 2 Violation all-bold tests  
  4. Fix P-33 fixture content + add meta-assertion
  5. Sweep existing parser tests for missing provenance assertions
  6. Run npm test, iterate until 0 failures

  Ref files: mcp/artifact-server/src/tools/analysis.ts:153-157, mcp/artifact-server/src/__tests__/tools.test.ts (multiple ranges).
resolution: null
status: pending
cycle_created: 28
cycle_modified: null
content_hash: a38cb1cd168d92bab37c189348a718ef71b12d977713f657b6a83f7011b67d2f
token_count: 907
