id: WI-854
type: work_item
title: Add tool_usage query method to local adapter
complexity: low
scope:
  - path: mcp/artifact-server/src/adapters/local/reader.ts
    op: modify
  - path: mcp/artifact-server/src/adapter.ts
    op: modify
  - path: mcp/artifact-server/src/adapters/__tests__/local-getToolUsage.test.ts
    op: create
depends:
  - WI-853
blocks: []
criteria:
  - "Local adapter reader exposes a method `getToolUsage(filter?: ToolUsageFilter): Promise<ToolUsageRow[]>` where ToolUsageFilter has optional fields: tool_name (string), session_id (string), cycle (number), phase (string), from (ISO timestamp), to (ISO timestamp)"
  - The query is a single parameterized SELECT — no N+1 fetch pattern
  - Method is added to StorageAdapter interface in adapter.ts
  - "Filter semantics: all present filters AND together; missing filters are ignored"
  - Results ordered by timestamp ASC, id ASC
  - A new test mcp/artifact-server/src/adapters/__tests__/local-getToolUsage.test.ts inserts N rows with varied tool_name/cycle/phase and asserts filter combinations return the expected subsets
  - TypeScript compile succeeds
domain: null
phase: PH-048
work_item_type: feature
notes: |-
  The reader is the only introspection path for tool_usage. This is the query surface backing WI-857's MCP tool.

  StorageAdapter interface extension:
  ```ts
  interface ToolUsageFilter {
    tool_name?: string;
    session_id?: string;
    cycle?: number;
    phase?: string;
    from?: string;
    to?: string;
  }
  getToolUsage(filter?: ToolUsageFilter): Promise<ToolUsageRow[]>;
  ```

  Remote adapter implementation is handled in WI-855.
resolution: null
status: done
cycle_created: null
cycle_modified: 27
content_hash: 6cf1bfab76261c36668b05fbdaa7a00886e12a4cf9b5d40e3298c9d31217784b
token_count: 450
