id: WI-900
type: work_item
title: "LocalAdapter public surface cleanup: context methods to interface or private"
complexity: small
scope:
  - path: mcp/artifact-server/src/adapter.ts
    op: edit
  - path: mcp/artifact-server/src/adapters/local/index.ts
    op: edit
  - path: mcp/artifact-server/src/adapters/remote/index.ts
    op: edit
  - path: mcp/artifact-server/src/validating.ts
    op: edit
depends:
  - WI-898
blocks: []
criteria:
  - The 5 LocalAdapter context methods (queryArchitectureDocument, queryGuidingPrinciples, queryConstraints, queryActiveProject, queryActivePhase) are either on StorageAdapter interface OR marked private
  - "If on interface: RemoteAdapter and ValidatingAdapter implement them"
  - Full vitest suite passes; tsc --noEmit passes
domain: null
phase: PH-054
work_item_type: maintenance
notes: Depends on WI-898 (adapter.ts coordination). Recommend promoting to interface since tools/context.ts uses them through the abstraction boundary.
resolution: Chose "remove rather than promote" path after inspecting tools/context.ts and finding the 5 LocalAdapter methods were dead forwarding — no caller used them through a StorageAdapter variable. Removed queryArchitectureDocument, queryGuidingPrinciples, queryConstraints, queryActiveProject, queryActivePhase from LocalAdapter. Methods remain on LocalContextAdapter (internal implementation class). LocalAdapter now strictly implements StorageAdapter with no additional public surface. 1820 tests pass, tsc clean. No RemoteAdapter / ValidatingAdapter changes needed.
status: done
cycle_created: 78
cycle_modified: 27
content_hash: 4c340a775364f4a8ae7778bacfa154c17a9997d943d2b370490b078d4345cb2a
token_count: 424
