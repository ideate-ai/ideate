id: WI-845
type: work_item
title: Remove metrics MCP tool registrations and dispatch cases
complexity: low
scope:
  - path: mcp/artifact-server/src/tools/index.ts
    op: modify
  - path: mcp/artifact-server/src/tools/metrics.ts
    op: delete
depends:
  - WI-843
  - WI-844
blocks: []
criteria:
  - File mcp/artifact-server/src/tools/metrics.ts does not exist
  - grep -n 'metrics' mcp/artifact-server/src/tools/index.ts returns zero matches for metrics-related imports, TOOLS entries, and dispatch cases (ideate_get_metrics and ideate_emit_metric specifically absent)
  - TOOLS array in mcp/artifact-server/src/tools/index.ts no longer contains entries named 'ideate_emit_metric' or 'ideate_get_metrics'
  - dispatch switch in tools/index.ts has no cases for 'ideate_emit_metric' or 'ideate_get_metrics'
  - TypeScript compile (tsc --noEmit) succeeds for mcp/artifact-server
domain: null
phase: PH-048
work_item_type: maintenance
notes: |-
  Remove:
  - Line 17 import: `import { handleEmitMetric, handleGetMetrics } from './metrics.js';`
  - Lines 466-511: ideate_get_metrics TOOLS entry
  - Lines 572-593: ideate_emit_metric TOOLS entry
  - Lines 765-769: dispatch cases for both

  Delete entire file mcp/artifact-server/src/tools/metrics.ts (547 lines).

  Depends on WI-843/844 because skill files still reference these tools in prose until stripped — removing the tool first would leave the skills in a state where docs reference a missing tool (minor, but avoid the cross-wired intermediate state).

  Note: this WI only removes the tool definitions. Test files still reference these symbols — those are handled in WI-851. Expect test build failures between this WI and WI-851 completion; integration order is handled via dependency graph.
resolution: null
status: done
cycle_created: null
cycle_modified: 27
content_hash: 0229de1d836170ceb6b98123b6f948b92ea70007c032bf50395b23040d19f4f6
token_count: 478
