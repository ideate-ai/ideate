id: WI-259
type: work_item
title: Fix domain-curator agent deprecated patterns
complexity: low
status: done
depends: []
blocks: []
scope:
  - {path: agents/domain-curator.md, op: modify}
criteria:
  - All {artifact-dir} placeholders changed to {project_root}
  - All references to .md files changed to YAML patterns
  - All references to domains/ subdirs reference YAML not .md files
  - Agent uses Read/Grep/Glob tools appropriately per P-06
notes: |
  Cycle 029 gap analysis found domain-curator agent uses deprecated patterns:

  1. Line 34: `{artifact-dir}` placeholder → change to `{project_root}`
  2. Line 36: `{artifact-dir}/.ideate/domains/*/policies/*.yaml` → should already be YAML, verify
  3. Line 38: `{artifact-dir}/.ideate/domains/*/decisions/*.yaml` → should already be YAML, verify
  4. Line 40: `{artifact-dir}/.ideate/domains/*/questions/*.yaml` → should already be YAML, verify
  5. Line 42: `{artifact-dir}/.ideate/principles/GP-*.yaml` → should already be YAML, verify
  6. Line 166-167: Creates `policies.md`, `decisions.md`, `questions.md` → should create YAML files per P-25

  Per P-25: No markdown files serve as source of truth. Per P-33 (path placeholder policy): {project_root} is canonical.

  Also fix Phase 5 header references from .md to .yaml:
  - Line 167: `policies.md` → `policies.yaml`
  - Line 173: `decisions.md` → `decisions.yaml`
  - Line 174: `questions.md` → `questions.yaml`

  Domain-curator is a critical agent used after every review cycle. These deprecated patterns cause it to create wrong file types.
