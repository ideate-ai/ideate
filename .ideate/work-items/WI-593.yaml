id: WI-593
type: work_item
title: Equivalence tests — query and aggregation operations
status: done
complexity: medium
scope:
  - path: mcp/artifact-server/src/__tests__/equivalence-query.test.ts
    op: create
depends:
  - WI-591
blocks: []
criteria:
  - queryNodes with type filter returns identical result sets for each NodeType present in the fixture
  - queryNodes with status filter returns identical results
  - queryNodes with domain filter returns identical results
  - queryNodes with phase filter returns identical results
  - queryNodes with combined filters (type + status) returns identical results
  - queryGraph from a known origin (1-hop outgoing) returns identical results
  - queryGraph from a known origin (2-hop both directions) returns identical results
  - countNodes grouped by status returns identical counts
  - countNodes grouped by type returns identical counts
  - countNodes grouped by domain returns identical counts
  - getDomainState() returns identical policy/decision/question sets per domain
  - getConvergenceData(cycle) returns identical finding counts and summary content
domain: agent-system
phase: PH-024
work_item_type: feature
notes: queryNodes and queryGraph results need to be sorted before comparison. countNodes should produce identical counts — any mismatch indicates a data loading discrepancy. getDomainState comparison should sort by ID. getConvergenceData requires the fixture to include at least one cycle with findings.
resolution: null
cycle_created: null
cycle_modified: 5
content_hash: 2bb205a64f82d2483aa7c9105420489a760e923c1d1e365cf59b7d9013996497
token_count: 406
