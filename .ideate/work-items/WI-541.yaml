id: WI-541
type: work_item
title: Add remaining documentation gaps in GUIDE.md and README.md
status: done
complexity: small
scope:
  - path: GUIDE.md
    op: modify
  - path: README.md
    op: modify
depends: []
blocks: []
criteria:
  - GUIDE.md triage section includes spike and maintenance type prefix examples alongside existing bug/feature/chore examples
  - "README.md includes a Context Loading table with rows for project (reads: project/phase, writes: project/phase/journal), triage (reads: phase/work_items, writes: work_items), and status (reads: workspace status, writes: none)"
  - No .ideate/ path references introduced beyond existing ones
domain: workflow
phase: null
work_item_type: feature
notes: |-
  # WI-541

  ## IR2: Triage examples
  In GUIDE.md triage section (~line 101), add two examples after the existing three:
  ```
  /ideate:triage spike: investigate whether we can use WebSockets for real-time updates
  /ideate:triage maintenance: upgrade TypeScript to 5.5 and fix type errors
  ```

  ## IR3: README Context Loading table
  Find the Context Loading table in README.md and add 3 rows:
  | `/ideate:project` | `ideate_artifact_query`, `ideate_get_workspace_status`, `ideate_get_artifact_context` | `ideate_write_artifact` (project, phase), `ideate_append_journal` |
  | `/ideate:triage` | `ideate_artifact_query` (phase, work_items) | `ideate_write_work_items` |
  | `/ideate:status` | `ideate_get_workspace_status` | None |
resolution: null
cycle_created: null
cycle_modified: 5
content_hash: 28741451cb1533579f4f98b19b91888cc81836fffa7cb0da97095e614e13c6a4
token_count: 402
