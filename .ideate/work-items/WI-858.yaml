id: WI-858
type: work_item
title: Write replacement report script querying ideate_get_tool_usage
complexity: low
scope:
  - path: scripts/report-tool-usage.sh
    op: create
depends:
  - WI-857
blocks: []
criteria:
  - scripts/report-tool-usage.sh exists and is executable (chmod +x)
  - "Script accepts optional flags: --cycle N, --phase PH-NNN, --session SID, --tool NAME, --from ISO, --to ISO"
  - "Script invokes the MCP server (or the adapter directly via a small TS helper) to call ideate_get_tool_usage and prints a human-readable summary: total calls, per-tool breakdown (count, tokens, bytes), and a top-5 most-called tools list"
  - Script exits non-zero and prints usage on invalid args
  - Running the script in a fresh workspace (no tool_usage rows) produces a well-formed 'no data' message and exits 0
  - README or script --help output documents each flag
domain: null
phase: PH-048
work_item_type: feature
notes: |-
  Per P-69 this WI replaces the four deleted scripts in WI-850. Together they form the remove-and-replace pair in the same change (phase).

  Implementation shape: bash + jq, calling the MCP server via stdio or via a small node invocation. Simplest path: `node mcp/artifact-server/dist/cli.js ideate_get_tool_usage --args '{...}'` if such a CLI exists, otherwise invoke via a short helper script. Choose the simpler path — this is a reporting tool, not production infrastructure.

  Do NOT recreate the complexity of report-cycle.sh / report-executive.sh. One script, one purpose: summarize tool usage.
resolution: null
status: done
cycle_created: null
cycle_modified: 27
content_hash: ef3c0ac6925bd31bd49be97fb52b23e34f821986a62e04d5b0e42462af4caf81
token_count: 425
