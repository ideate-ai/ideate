id: WI-263
type: work_item
title: Fix domain-curator path error
complexity: low
status: done
depends: []
blocks: []
scope:
  - {path: agents/domain-curator.md, op: modify}
criteria:
  - Line 166 references {project_root}/.ideate/domains/ not {project_root}/domains/
  - Domain curator creates directories in correct location
notes: |
  Full audit 20260326 found critical path error:

  Line 166 currently says:
  "1. Create the directory `{project_root}/domains/{name}/`"

  Should be:
  "1. Create the directory `{project_root}/.ideate/domains/{name}/`"

  This would cause the domain-curator to create domains at the project root level instead of under .ideate/.
