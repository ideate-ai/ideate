id: WI-564
type: work_item
title: Add missing metrics design sections and archiveCycle mapping to GraphQL schema
status: done
complexity: small
scope:
  - path: docs/platform/graphql-schema.md
    op: modify
depends: []
blocks: []
criteria:
  - "Design section added for emitEvent mutation: purpose, input type, return type, auth scope"
  - "Design section added for emitMetric mutation: purpose, input type, return type, auth scope"
  - "Design section added for metrics query: purpose, input parameters, return type, auth scope"
  - "archiveCycle row added to Section 11 StorageAdapter-to-GraphQL mapping table: archiveCycle(cycle) → mutation { archiveCycle(cycleNumber) }"
  - All Section 10 mapping table entries now have corresponding design sections (P-54 compliance verified)
domain: workflow
phase: PH-018
work_item_type: bug
notes: |-
  Resolves full audit gap-analyst finding (3 metrics ops without design sections) and spec-reviewer finding (archiveCycle missing from Section 11).

  For the 3 metrics design sections, add as Sections 3.14 (metrics query), 5.9 (emitEvent), and 5.10 (emitMetric). Each needs: purpose (1 sentence), input type definition, return type, auth scope. Follow the pattern established by Sections 3.8-3.13 and 5.6-5.8.

  For Section 11: add row mapping archiveCycle(cycle: number): Promise<void> to mutation { archiveCycle(cycleNumber: Int!): Boolean! }.
resolution: null
cycle_created: 8
cycle_modified: 5
content_hash: 8f1bee03f51d6e68612c90a5a7256999277146bd0587736d29d0b30706d18b45
token_count: 385
