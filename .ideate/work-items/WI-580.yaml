id: WI-580
type: work_item
title: Categorize migration edge errors from initial import
status: done
complexity: medium
scope:
  - path: docs/platform/migration-diagnostic-report.md
    op: create
depends: []
blocks: []
criteria:
  - Re-run migration with verbose/debug output capturing every error
  - "Each of the 712 edge errors categorized into buckets: missing target node, invalid edge type, type mismatch, dangling reference in source YAML, other"
  - Per-bucket counts documented with representative examples
  - Root cause identified for each bucket (migration tool bug, source data issue, edge extraction logic)
  - "Recommendations: which buckets are fixable in migration tool vs source data cleanup vs both"
domain: artifact-structure
phase: PH-022
work_item_type: spike
notes: |-
  Re-run migration CLI against ideate/.ideate/ with maximum verbosity. Check for --verbose or --debug flag. If not available, add temporary error logging to writer.ts.

  **Server startup**: Before running the migration, start the server and Neo4j if not already running:
  ```bash
  cd /Users/dan/code/ideate-server && docker compose up -d neo4j && sleep 5 && npm run dev &
  ```
  Verify with: `curl -s http://localhost:4000/graphql -H 'Content-Type: application/json' -d '{"query":"{ __typename }"}' `
  If the server is not reachable, start it before proceeding. Do not skip this step.

  Expected error categories:
  - Missing target: edge references a node ID that doesn't exist
  - Type mismatch: edge connects incompatible node types
  - Dangling YAML reference: source YAML references obsoleted/deleted artifacts
  - Parse failure: edge extraction logic fails on particular field formats

  Output: docs/platform/migration-diagnostic-report.md with categorized findings.
resolution: null
cycle_created: 12
cycle_modified: 5
content_hash: eb30ecfb15cea413fb6ad8a4b782db402de75f07d7bd54d3fb21b9032b0d0036
token_count: 480
