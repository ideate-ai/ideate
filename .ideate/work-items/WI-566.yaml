id: WI-566
type: work_item
title: Wire LocalAdapter into server.ts and make ctx.adapter non-optional
status: done
complexity: medium
scope:
  - path: mcp/artifact-server/src/server.ts
    op: modify
  - path: mcp/artifact-server/src/types.ts
    op: modify
depends: []
blocks: []
criteria:
  - initServer constructs LocalAdapter from db/drizzleDb/ideateDir and assigns to state.ctx.adapter
  - selectAdapter returns StorageAdapter instead of void
  - ToolContext.adapter changed from optional (?) to required
  - All existing tests pass (630+)
  - "Integration test added: after initServer, ctx.adapter is non-null and instanceof LocalAdapter"
domain: artifact-structure
phase: PH-019
work_item_type: bug
notes: Critical fix. server.ts line 138 builds ctx without adapter. Change selectAdapter to construct and return a LocalAdapter for 'local' backend. Assign to state.ctx.adapter. Change types.ts adapter field from optional to required. Existing tool handler fallback branches (else paths) serve as backward compat for tests that build ctx manually without an adapter — keep them for now but they become dead code in production.
resolution: null
cycle_created: 9
cycle_modified: 5
content_hash: a5054b46b412972beb2133637c85091eefacbeb24b10db9c5c611645319f2b92
token_count: 320
