id: WI-869
type: work_item
title: Document resolved-findings filter contract and fix equivalence fixture
complexity: small
scope:
  - path: mcp/artifact-server/docs/adapter-contract.md
    op: modify
  - path: docs/platform/adapter-interface.md
    op: modify
  - path: mcp/artifact-server/src/adapter.ts
    op: modify
  - path: mcp/artifact-server/src/tools/index.ts
    op: modify
  - path: mcp/artifact-server/tests/adapters/equivalence-query.test.ts
    op: modify
depends: []
blocks: []
criteria:
  - "mcp/artifact-server/docs/adapter-contract.md getConvergenceData section (~lines 412-422) adds a clause: 'Only findings where addressed_by is null are counted. Findings with a non-null or empty-string addressed_by are considered resolved and must be excluded from all counts.' Applies to both LocalAdapter and RemoteAdapter implementations."
  - docs/platform/adapter-interface.md getConvergenceData section (~lines 406-413) adds the same filtering clause.
  - "mcp/artifact-server/src/adapter.ts getConvergenceData JSDoc (lines 480-487) adds a one-line note: 'Only findings where addressed_by is null are counted; resolved findings (non-null addressed_by) are excluded.'"
  - "mcp/artifact-server/src/tools/index.ts ideate_get_convergence_status tool description (~line 185) changes to: 'Convergence status: unresolved findings by severity (resolved findings excluded), principle verdict. Use to check if cycle converged.' Word count stays under 50 per P-34."
  - "mcp/artifact-server/tests/adapters/equivalence-query.test.ts:404-405: assertion for localTotal and remoteTotal changed from `toBeGreaterThanOrEqual(1)` to `toBe(0)` — the fixture finding F-WI-001-001 has addressed_by: WI-001 and is therefore resolved post-WI-860. Update the comment accordingly."
  - "All existing tests pass: `npm test` in mcp/artifact-server/ returns zero failures."
domain: artifact-structure
phase: PH-049
work_item_type: maintenance
notes: |-
  Follow-up to WI-860. Cycle 030 capstone gap-analyst flagged II1 (equivalence fixture assertion inverted post-WI-860), II2 (adapter contract docs missing filtering guarantee), II3 (interface JSDoc missing filtering contract), and MI1 (tool description misleading). All four are documentation/fixture gaps that must align with the WI-860 change and P-88 policy.

  VERIFY in equivalence-query.test.ts: the fixture at tests/adapters/fixtures/equivalence/.ideate/cycles/001/findings/F-WI-001-001.yaml has `addressed_by: WI-001`. After WI-860, getConvergenceData excludes this finding. The test at line 404 currently asserts `expect(localTotal).toBeGreaterThanOrEqual(1)` which is now logically inverted.

  P-34: keep tool description under 50 words. Current description is ~13 words; new version should be similar.

  Derived from: P-88 (resolved-finding exclusion), D-213 (addressed_by canonical null).
resolution: "Cycle 31 fix: added resolved-finding exclusion clause to adapter-contract.md and adapter-interface.md; added exclusion line to getConvergenceData JSDoc in adapter.ts; rewrote ideate_get_convergence_status tool description (dropped misleading 'addressed counts'); fixed inverted assertions in equivalence-query.test.ts:404 from toBeGreaterThanOrEqual(1) to toBe(0) for both local and remote sides."
status: done
cycle_created: 30
cycle_modified: 27
content_hash: 2ac71429a9565c5e6c3ef97044dfd2a82344c48533cf69c74acf7923489332d2
token_count: 849
