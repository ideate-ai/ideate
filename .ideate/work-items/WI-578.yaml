id: WI-578
type: work_item
title: Add adapter path to handleGetMetrics and remove dead fallback code
status: done
complexity: small
scope:
  - path: mcp/artifact-server/src/tools/metrics.ts
    op: modify
  - path: mcp/artifact-server/src/tools/query.ts
    op: modify
depends: []
blocks: []
criteria:
  - handleGetMetrics has adapter-first branch using adapter.queryNodes or adapter.countNodes
  - Inlined upsert helpers in metrics.ts removed (dead code — adapter always set in production)
  - handleEmitMetric fallback path removed or clearly marked as test-only
  - "query.ts: runFilterMode and runGraphMode fallback paths clearly marked as test-only with comments"
  - No functional changes to production behavior
  - All existing tests pass
domain: artifact-structure
phase: PH-021
work_item_type: maintenance
notes: |-
  Two changes:
  1. metrics.ts handleGetMetrics (~line 692) uses ctx.db directly. Add if(ctx.adapter) branch. The inlined helpers (lines 43-58) are dead in production since handleEmitMetric always uses adapter.putNode. Remove them or mark clearly as test-only.
  2. query.ts: runFilterMode and runGraphMode are fallback paths that only execute when ctx.adapter is absent. In production, the adapter path in handleArtifactQuery runs instead. Add clear comments marking them as test-only fallbacks.
resolution: null
cycle_created: 12
cycle_modified: 5
content_hash: b66a5097d3d5459853018eb2e2d12c9b9adbcdc1dd40da6f4eea52f10c1940f2
token_count: 367
