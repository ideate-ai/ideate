id: WI-751
type: work_item
title: Fix schema.test.ts stale extension table count
complexity: low
scope:
  - path: mcp/artifact-server/src/__tests__/schema.test.ts
    op: modify
depends: []
blocks: []
criteria:
  - '"proxy_human_decisions" appears in the extensionTables array'
  - The count assertion reads .toBe(16)
  - npm test -- schema.test.ts passes with no failures
  - No other changes to schema.test.ts
domain: artifact-structure
phase: null
work_item_type: bug
notes: |-
  ## Context

  WI-748 added the `proxy_human_decisions` table to `schema.ts` (line 612) and its index (line 624). The table is exported from `db.ts` (line 164) and mapped in `TYPE_TO_EXTENSION_TABLE`. However `schema.test.ts` was never updated.

  ## Fix

  In `src/__tests__/schema.test.ts`, around lines 83-108:

  1. Add `"proxy_human_decisions"` to the `extensionTables` array (insert in alphabetical position after `"projects"`).
  2. Change the assertion from `.toBe(15)` to `.toBe(16)`.

  The test description string also says "creates all 15 extension tables" — update it to "creates all 16 extension tables".

  No other changes needed. The test body already iterates the array and checks each table exists.
resolution: Added proxy_human_decisions to extensionTables array and updated count assertion to .toBe(16). One out-of-scope edit (checkSchemaVersion test description) reverted during review.
status: done
cycle_created: 22
cycle_modified: 21
content_hash: 571e44ab5b0791441eae89955c888e214d2276721ce50cf98a0e95941f483cd2
token_count: 384
