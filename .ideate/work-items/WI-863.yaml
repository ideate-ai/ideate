id: WI-863
type: work_item
title: Add getToolUsage contract coverage to adapter-contract.test.ts
complexity: small
scope:
  - path: mcp/artifact-server/test/adapter-contract.test.ts
    op: modify
depends:
  - WI-861
blocks: []
criteria:
  - adapter-contract.test.ts contains a contract test for getToolUsage covering aggregate view, detail view, and both view.
  - Contract test exercises filters by session_id, cycle, phase, and limit handling.
  - Both LocalAdapter and RemoteAdapter pass the contract (existing test harness runs both).
  - Test fails if either adapter returns incorrect shape, missing context fields, or inconsistent truncation behavior across views.
domain: null
phase: PH-048
work_item_type: feature
notes: "Derived from cycle 28 code-reviewer finding M3. getToolUsage is the only adapter method missing from the contract test. Pattern: follow existing tests in the file (e.g. getMetricsEvents before its removal, getArtifact, getFindings). Soft dependency on WI-861 — contract test should exercise the wired columns, so complete WI-861 first."
resolution: Added 14 contract tests to adapter-contract.test.ts covering aggregate/detail/filter-combination views, round-tripping session_id/cycle/phase, and no-silent-truncation. Added ValidatingAdapter.getToolUsage output validation (id/tool_name/timestamp) plus negative-shape test. Clarified in comments that limit is a handler concern not adapter concern. 107/107 tests pass. Review Pass after one rework round (initial Fail on AC-4 negative-test gap and AC-2 filter ambiguity).
status: done
cycle_created: null
cycle_modified: 27
content_hash: 4c2727e4d5c069f4110555d9099bf2adcdfde2d60917c08462a223906ea7344c
token_count: 425
