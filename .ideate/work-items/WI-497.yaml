id: WI-497
type: work_item
title: Add default_appetite and circuit_breaker_threshold to /ideate:settings
status: done
complexity: small
scope:
  - path: skills/settings/SKILL.md
    op: modify
depends: []
blocks: []
criteria:
  - The settings skill interactive menu includes a category for project settings (or integrates into existing categories) exposing default_appetite and circuit_breaker_threshold
  - default_appetite shows current value, default (6), and accepts positive integer input
  - circuit_breaker_threshold shows current value, default (5), and accepts positive integer input
  - The argument shortcut path works (e.g., /ideate:settings appetite or /ideate:settings circuit-breaker)
  - Existing settings categories (agent budgets, PPR, spawn mode) are preserved
domain: workflow
phase: null
notes: |-
  ## What to change

  ### skills/settings/SKILL.md
  Add a new Phase 2d section (or extend an existing section) for project-level settings:

  1. **Menu entry**: Add 'd) Project Settings' to the interactive menu alongside existing a) Agent, b) PPR, c) Spawn Mode options.

  2. **Phase 2d content**: Present current values of default_appetite and circuit_breaker_threshold (read from ideate_get_config). Allow the user to change each. Write changes via ideate_update_config.

  3. **Argument shortcuts**: Add 'appetite' and 'circuit-breaker' as argument shortcuts that jump directly to the relevant setting.

  Also fix the Phase 2c reference — the skill mentions Phase 2c for spawn mode but the section may not exist. Verify and add if missing.

  ## Why
  Gap-analyst S2: Users have no discoverable UI path to change these config values. They exist in handleUpdateConfig but not in the settings skill.
resolution: null
cycle_created: null
cycle_modified: 5
content_hash: b9f5d456d7b635f7b97895aecb316b53321ff269e996fff18c63fae0b8f20885
token_count: 444
