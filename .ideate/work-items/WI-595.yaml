id: WI-595
type: work_item
title: Equivalence tests — batch, lifecycle, and sequence operations
status: done
complexity: small
scope:
  - path: mcp/artifact-server/src/__tests__/equivalence-batch.test.ts
    op: create
depends:
  - WI-591
blocks: []
criteria:
  - batchMutate with 3+ nodes and 2+ edges produces equivalent results from both adapters
  - batchMutate validation errors (DAG cycle) are reported equivalently
  - nextId returns the same next ID for each NodeType given identical starting state
  - archiveCycle produces equivalent outcomes (both succeed, counts match)
  - appendJournalEntry creates a journal entry retrievable by getNode from both adapters
  - initialize() and shutdown() complete without error on both adapters
domain: agent-system
phase: PH-024
work_item_type: feature
notes: batchMutate and nextId tests should run AFTER read-only tests to avoid polluting fixture state. nextId comparison requires both backends to start from the same state. archiveCycle summary strings may differ in wording — compare structured outcome (counts) rather than exact strings.
resolution: null
cycle_created: null
cycle_modified: 5
content_hash: c5ccf174ff0f339ffbf5198924f72659c71a155132027386d1b146ed7cf9bc59
token_count: 310
