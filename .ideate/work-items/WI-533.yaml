id: WI-533
type: work_item
title: Fix deferred minor items from cycle 6-7 reviews
status: done
complexity: small
scope:
  - path: scripts/migrate-to-hierarchy.sh
    op: modify
  - path: mcp/artifact-server/src/tools/index.ts
    op: modify
  - path: skills/status/SKILL.md
    op: modify
depends: []
blocks: []
criteria:
  - migrate-to-hierarchy.sh sed fallback uses literal newlines instead of \n escape (BSD sed compatible)
  - ideate_append_journal inputSchema skill enum includes 'project' and 'triage' in addition to existing values
  - "skills/status/SKILL.md frontmatter includes model: sonnet"
  - npm test passes with zero failures
  - No other behavioral changes
domain: workflow
phase: null
work_item_type: feature
notes: |-
  # WI-533: Fix deferred minor items

  ## M2: sed fallback (migrate-to-hierarchy.sh)
  Lines 280,283 use `\n` in sed replacement strings. BSD sed (macOS) outputs literal backslash-n. Fix: use ANSI-C quoting ($'\n') or a shell variable with embedded newline.

  ## II1: Journal skill enum (index.ts)
  The ideate_append_journal inputSchema at ~line 261 restricts skill to ["plan", "execute", "review", "refine", "autopilot"]. Add "project" and "triage" to the enum array.

  ## IR1: Status skill model (status/SKILL.md)
  Frontmatter is missing model declaration. Add `model: sonnet` for consistency with other skills.
resolution: null
cycle_created: null
cycle_modified: 5
content_hash: 43e6750684b1ce6f6a48822deed7ff87370a27a1fedfed3aaac2c335ae302fc1
token_count: 376
