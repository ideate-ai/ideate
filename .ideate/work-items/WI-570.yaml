id: WI-570
type: work_item
title: Update ARCHITECTURE.md for adapter layer and sync specs
status: done
complexity: medium
scope:
  - path: ARCHITECTURE.md
    op: modify
  - path: docs/platform/adapter-interface.md
    op: modify
depends: []
blocks: []
criteria:
  - "ARCHITECTURE.md ToolContext definition includes adapter?: StorageAdapter field"
  - ARCHITECTURE.md has StorageAdapter section describing the adapter pattern, LocalAdapter composition, and data flow through the adapter
  - ARCHITECTURE.md source files table includes adapter.ts, adapters/local/writer.ts, adapters/local/reader.ts, adapters/local/context.ts, adapters/local/index.ts
  - adapter-interface.md updated to include appendJournalEntry method specification
  - adapter-interface.md archiveCycle return type updated from void to string with rationale
  - adapter.ts JSDoc for appendJournalEntry and archiveCycle cleaned of YAML/SQLite references (P-53)
domain: artifact-structure
phase: PH-019
work_item_type: maintenance
notes: |-
  Resolves: ARCHITECTURE.md stale (MI1/D2/N2/IR1), appendJournalEntry undocumented (D4/U1/II1), archiveCycle return type mismatch (D3), P-53 JSDoc violations.

  For ARCHITECTURE.md: add Section 5.1 'StorageAdapter Pattern' after the existing MCP server section. Include: adapter.ts interface overview, LocalAdapter composition (writer+reader+context), data flow showing tool handler → adapter → YAML+SQLite, ToolContext with adapter field. Update source files table.

  For adapter-interface.md: add appendJournalEntry to Section 3 with signature matching adapter.ts. Update archiveCycle return type from void to string. Note the deviation rationale (response string needed by MCP tool handler).

  For adapter.ts: clean JSDoc on appendJournalEntry (remove 'YAML write, SQLite upsert') and archiveCycle (remove file-movement language). Use storage-agnostic terms per P-53.
resolution: null
cycle_created: 11
cycle_modified: 5
content_hash: be52e2872fa83afd5fd2bf425cafc43c13e65ef3b5a286cc15032f64cba45f2b
token_count: 507
