id: WI-495
type: work_item
title: Add journal_entry redirect test for handleWriteArtifact
status: done
complexity: low
scope:
  - path: mcp/artifact-server/src/__tests__/tools.test.ts
    op: modify
depends:
  - WI-494
blocks: []
criteria:
  - A test calls handleWriteArtifact with type journal_entry and valid journal fields (skill, date, entry_type, body, cycle_number)
  - The test asserts a journal_entry node is created in SQLite
  - Existing tests pass without regression
domain: artifact-structure
phase: null
notes: |-
  ## What to add

  ### tools.test.ts
  Add a test:
  ```typescript
  it('redirects journal_entry to handleAppendJournal', async () => {
    await handleWriteArtifact(ctx, {
      type: 'journal_entry',
      id: 'J-001-001',
      content: {
        skill: 'execute',
        date: '2026-03-31',
        entry_type: 'test',
        body: 'body text',
        cycle_number: 1,
      },
    });
    const row = db.prepare("SELECT type FROM nodes WHERE type = 'journal_entry'").get();
    expect(row).toBeDefined();
  });
  ```

  ## Why
  Code-reviewer S3: The journal_entry redirect path has no test coverage.
resolution: null
cycle_created: null
cycle_modified: 5
content_hash: 074f55eab999eb0e7b252886ec1ccf303492fb6978b1ddee478f92e7a98cc228
token_count: 298
