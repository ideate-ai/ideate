id: WI-696
type: work_item
title: Add INVALID_NODE_ID, INVALID_LIMIT, INVALID_OFFSET guards to LocalReaderAdapter
complexity: small
scope:
  - path: mcp/artifact-server/src/adapters/local/reader.ts
    op: modify
  - path: mcp/artifact-server/tests/adapters/local-adapter-validation.test.ts
    op: modify
depends: []
blocks:
  - WI-700
criteria:
  - getNode('') throws ValidationError with code INVALID_NODE_ID. Verified by test.
  - getNode(null) throws ValidationError with code INVALID_NODE_ID. Verified by test.
  - "queryNodes({limit: -1}) throws ValidationError with code INVALID_LIMIT. Verified by test."
  - "queryNodes({offset: -1}) throws ValidationError with code INVALID_OFFSET. Verified by test."
  - "queryGraph({limit: -1}) throws ValidationError with code INVALID_LIMIT. Verified by test."
  - "queryGraph({offset: -1}) throws ValidationError with code INVALID_OFFSET. Verified by test."
  - Validation logic mirrors the existing LocalAdapter facade at src/adapters/local/index.ts:89-118.
  - All existing reader tests continue to pass. Test suite passes with zero regressions.
domain: adapter-equivalence
phase: null
work_item_type: bug
notes: |-
  Add guards at the top of getNode (line 278), queryNodes (line 394), and queryGraph (line 527) in reader.ts. Use the same ValidationError codes as the facade in index.ts:89-118 — INVALID_LIMIT, INVALID_OFFSET, INVALID_NODE_ID. The facade checks `limit < 0` and `offset < 0`; apply identical conditions here.

  For getNode: `if (!id || typeof id !== 'string') throw new ValidationError('INVALID_NODE_ID', 'Node ID must be a non-empty string')`.

  Add tests to local-adapter-validation.test.ts, following the existing test pattern in that file. Tests must use the LocalReaderAdapter directly (not through the LocalAdapter facade) to verify the reader's own guards are active.
resolution: null
status: done
cycle_created: 21
cycle_modified: 19
content_hash: a0da3d04392474b1a8d41bc520849ee0dd5556a1dd2bfee63e4747fb3eb61252
token_count: 501
