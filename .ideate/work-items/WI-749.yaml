id: WI-749
type: work_item
title: "Spike: investigate domain_index node type — planned but unimplemented or dead code"
complexity: low
scope:
  - path: mcp/artifact-server/src/
    op: read
  - path: mcp/artifact-server/tests/
    op: read
  - path: skills/
    op: read
  - path: agents/
    op: read
depends: []
blocks: []
criteria:
  - Search the entire ideate codebase (skills, agents, mcp/artifact-server/src, tests, docs) for every occurrence of the string 'domain_index'
  - Determine whether domain_index was an intended node type that was never added to ALL_NODE_TYPES in adapter.ts, or references dead/removed code
  - "Produce a D-NNN domain decision documenting the finding and the resolution: (a) if planned but missing — create follow-on work item to add it properly (type definition, ALL_NODE_TYPES, schema table, indexer extraction); (b) if dead code — identify and remove all references; (c) if non-issue — document why and close Q-145"
  - Q-145 is updated to resolved status referencing the decision
domain: artifact-structure
phase: PH-040
work_item_type: spike
notes: |-
  Origin: the code-reviewer (which ran out of budget during the full audit) left a partial observation about domain_index ID validation. The architect confirmed domain_index does not appear in ALL_NODE_TYPES in adapter.ts. Q-145 was written to track the open question.

  This is a read-only spike. No code changes unless the investigation uncovers dead code that is safe to remove as part of this work item (low-risk, clearly unused references). If the investigation points to a larger feature gap, produce only the decision and a follow-on work item spec — do not implement the feature in this spike.
resolution: Spike investigation concluded domain_index is fully implemented throughout the stack (adapter.ts, ALL_NODE_TYPES, LocalAdapter reader, RemoteAdapter). Not a bug or dead code. The stale assertion count in always-include-types-validation.test.ts (27 vs actual 28) was the real issue from WI-745. D-190 written, Q-145 closed.
status: done
cycle_created: 21
cycle_modified: 20
content_hash: 68508b43786dce2695c3cb777d23088d3ae4f20f256b49cdfd8e48968203b843
token_count: 544
