id: overview
type: overview
title: "PH-025 Refinement: Fix remaining 35 equivalence test failures"
description: Fix all divergences between LocalAdapter and RemoteAdapter. Zero accepted differences. Both backends must produce identical results for all 21 StorageAdapter methods.
what_is_changing: LocalAdapter edge extraction gaps (missing BELONGS_TO_DOMAIN for domain_decision/question). Server query filter behavior (domain/phase filters, countNodes, getDomainState, convergenceData). Server graph traversal (BFS and PPR containment edge leaks). Write path alignment (putNode/patchNode/batchMutate/nextId/archiveCycle/appendJournalEntry).
why: PH-025 Production Validation revealed 35/170 equivalence test failures. All trace to genuine backend divergences, not test bugs. The project acceptance criterion requires 100% parity.
scope_boundary: Fix adapter and server code only. No changes to the equivalence test assertions or comparison helpers — the tests are correct, the backends need to match.
work_items:
  - "WI-599: Edge extraction parity (LocalAdapter indexer)"
  - "WI-600: Server query/aggregation alignment"
  - "WI-601: Server BFS and PPR containment edge filtering"
  - "WI-602: Write path alignment (putNode, batchMutate, nextId, etc.)"
content_hash: 4c5cda3655faeb297c24cbbda73684a0aeab3963253ed0a77a3d407487cb8710
token_count: 313
