id: P-06
type: domain_policy
amended: cycle 029 — clarified that agent instructions must use MCP exclusively, no direct file writes
amended_by: null
cycle_created: 1
cycle_modified: 29
description: |-
  All inter-phase state resides in YAML files in the `.ideate/` directory. No in-memory state carries between skill invocations. Each skill starts cold and reads what it needs exclusively through the ideate MCP artifact server. Skills do not read or write YAML files directly — the MCP server is the mandatory interface between skills and the artifact directory. The SQLite index is a derived cache backing the MCP tools; skills never interact with it directly.

  **Agent requirement**: Agent markdown files (agents/*.md) must not instruct agents to use direct file write tools (e.g., Write, Edit). Agents must use MCP tools (`ideate_write_artifact`, etc.) for all artifact mutations.
domain: artifact-structure
established: planning phase
status: active
content_hash: ce4475824af3d04d1960d6a95ff86dfcc7a285da1dbdde0e950861fd13df98c2
token_count: 239
file_path: .ideate/domain_file/domains/artifact-structure/policies/P-06.yaml.yaml
