id: F-WI-746-001
type: finding
work_item: WI-746
verdict: Pass
content: |-
  ## Verdict: Pass (after rework)

  Decision null-status test correct. Question test was tautological (domain never entered result map) — fixed by anchoring domain with a decision node.

  ## Critical Findings

  None.

  ## Significant Findings

  ### S1: Question null-status test always passed via tautological else-branch (fixed)
  - **File**: `src/__tests__/adapter-contract.test.ts:1230-1249`
  - **Issue**: Null-status question excluded from domainSet by SQL filter, so getDomainState returned empty Map. if(entry) was never true; else branch asserted toBeUndefined() trivially. Test could never fail regardless of implementation behavior.
  - **Resolution**: Added anchor domain_decision node to guarantee domain appears in result map. Test now asserts entry.questions does not contain Q-null-status.

  ## Minor Findings

  ### M1: Contract description ambiguous on decision status filtering
  - **File**: `docs/adapter-contract.md` (not modified)
  - **Issue**: Says 'active policies, decisions, and open questions' — 'active' is ambiguous about whether it applies to decisions. SQL has no status filter on decisions.
  - **Recommendation**: Defer — clarification only; no functional gap.

  ## Unmet Acceptance Criteria

  None (after rework).
cycle: 21
content_hash: 6e3c684fad07e3447849d7a14095346fbb030101292d5ac966535f8ffab7f015
token_count: 335
