id: F-WI-844
type: finding
work_item: WI-844
reviewer: code-reviewer
verdict: Pass (after rework)
severity_counts:
  critical: 0
  significant: 2
  minor: 2
findings:
  - severity: significant
    id: S1
    description: "autopilot/phases/review.md retained a full '### Emit Quality Summary' section (header, note block, and 5 derivation sub-steps: Severity counts, Per-reviewer counts, Category counts, work_items_reviewed, andon_events) after the emit_metric call the derivations fed was removed. The section described how to compute a payload with no consumer — internally incoherent."
    resolution: Removed the entire '### Emit Quality Summary' section, and renamed the downstream '### Spawn Domain Curator (After Quality Summary Emitted)' header to '### Spawn Domain Curator' to drop the stale parenthetical referencing the deleted section.
  - severity: significant
    id: S2
    description: autopilot/phases/review.md contained a journal template block 'Also append a per-cycle metrics summary' referencing wall_clock_ms, per-agent spawn counts, and slowest-agent derivation — all of which depended on the removed metrics collection path.
    resolution: Removed the cycle-level metrics summary journal template from review.md.
  - severity: minor
    id: M1
    description: autopilot/phases/reporting.md Phase 9 retained an 'Overall metrics summary' journal template referencing Total agents spawned / Total wall-clock / Cycles executed — data no longer collected.
    resolution: Removed the overall metrics summary journal template block from reporting.md Phase 9.
  - severity: minor
    id: M2
    description: "Unmet acceptance criterion: review.md still referenced a metrics emission path via its Quality Summary derivations after the emit call was removed."
    resolution: Addressed by S1 fix — removing the Emit Quality Summary section resolved the AC.
rework_applied: true
notes: "All review findings in scope (within WI-844's file scope: skills/autopilot/**). Significant findings fixed within scope per Phase 8 handling; no Andon cord required. Verified via grep: zero emit_metric / wall_clock_ms / Slowest agent / 'Agents spawned' references remain under skills/."
cycle: 71
cycle_modified: 27
content_hash: 928f7ebfbcf0383afa7c86d3fae5c452679a449c8ff3940d1a5e13d1c06141f8
token_count: 555
