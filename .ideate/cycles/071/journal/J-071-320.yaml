id: J-071-320
type: journal_entry
phase: execute
date: 2026-04-09
cycle_created: 71
title: work-item-complete
content: >-
  ## WI-734: Fill two Q-120 testing gaps — nextId unsupported type and
  deprecated policy exclusion


  **Verdict**: Pass (incremental review, M1 pre-existing/out-of-scope)


  Two testing gaps from Q-120 (cycle 17 gap analysis SG2 + MG3) were filled:


  **Gap 1 — INVALID_NODE_TYPE path**: Added 2 tests in
  `nextid-cycle-validation.test.ts` using `LocalWriterAdapter` directly (not
  `LocalAdapter`, which routes to the reader for non-cycle types). Tests call
  `nextId("domain_policy")` and `nextId("guiding_principle")` cast as NodeType
  and assert `code === 'INVALID_NODE_TYPE'`. The path is at `writer.ts:519–524`.


  **Gap 2 — Deprecated/superseded policy exclusion**: Added 1 test in
  `tools.test.ts` in the existing `handleGetDomainState` describe block. Test
  inserts active, deprecated, and superseded `domain_policy` nodes, calls
  `handleGetDomainState`, and asserts only the active policy appears in the
  result. Exercises the filter at `analysis.ts:239`.


  **Stale assertion check**: Confirmed line 233 in
  `nextid-cycle-validation.test.ts` already uses `INVALID_CYCLE` (corrected in a
  prior cycle); no update needed.


  **Minor finding from reviewer (pre-existing, not introduced by WI-734)**:
  `scripts/pretest.js` calls `process.exit(1)` on stale migration script
  detection, blocking `npm test`. Tests verified via `npx vitest run` instead.
  Pre-existing issue outside this work item's scope.


  **Test result**: 1166 passed, 0 failed (3 new tests). P-49: test-only changes.


  **Worktree**: `/private/tmp/ideate-wi-734`, branch `ideate/734-testing-gaps`
