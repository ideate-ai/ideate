id: J-071-336
type: journal_entry
phase: refine
date: 2026-04-10
cycle_created: 71
title: refinement-planned
content: >-
  ## Cycle 21 Refinement — Polish cycle addressing cycle 20 minor findings


  **Trigger**: Cycle 20 capstone review passed with 3 carry-forward minor
  findings (MN1, MN2, MN3) that were deferred. This refinement addresses all
  three.


  **Work items produced**: WI-745, WI-746


  ### WI-745 — Test polish: dead sentinel + loose assertion

  - Remove lines 23-29 of
  `tests/adapters/always-include-types-validation.test.ts` (dead compile-time
  sentinel — never evaluated by tsc because tests/ is excluded from tsconfig
  include)

  - Tighten runtime length assertion from `toBeGreaterThan(0)` to `toBe(27)`
  with updated comment


  ### WI-746 — getDomainState null-status coverage (decisions + questions)

  - Add test for null-status domain_decisions in getDomainState (decisions have
  no status filter — null-status items are included)

  - Add test for null/absent-status domain_questions — executor must verify
  correctness against adapter-contract.md before writing assertions; the SQL at
  reader.ts:868-875 uses `WHERE n.status = 'open'` which excludes null-status
  questions, but the contract document governs intent


  ### Scope boundary

  - Only `tests/adapters/always-include-types-validation.test.ts` and
  `src/__tests__/adapter-contract.test.ts` are modified

  - No architecture changes

  - Guiding principles unchanged


  **Execution strategy**: Sequential (2 independent low-complexity items)
