id: J-071-321
type: journal_entry
phase: execute
date: 2026-04-09
cycle_created: 71
title: work-item-complete
content: >-
  ## WI-735: Close 12 stale open questions resolved in prior cycles


  **Verdict**: Pass (incremental review, M1 cosmetic title fix applied)


  12 open questions (Q-117 through Q-138, non-contiguous) were updated to
  `status: closed` via MCP with resolution notes verified against codebase
  evidence. Open question count: 19 → 7.


  **Questions closed**:

  - Q-117: debounce (ArtifactWatcher, 500ms, pendingChanged Set)

  - Q-118: file_path index (idx_nodes_file_path at schema.ts:428)

  - Q-120: testing gaps (addressed by WI-734 this cycle)

  - Q-124: archiveCycle interface (adapter.ts:457, both adapters)

  - Q-131: INVALID_CYCLE in nextId (writer.ts:475,483)

  - Q-132: dead type constraint removed from adapter-contract.md patchNode
  section

  - Q-133: INVALID_NODE_ID guard for deleteNode in both adapters

  - Q-134: MISSING_EDGE_SOURCE/TARGET/TYPE + INVALID_EDGE_TYPE guards for
  putEdge in both adapters

  - Q-135: INVALID_NODE_ID + INVALID_EDGE_TYPE guards for removeEdges in both
  adapters

  - Q-136: PARSE_ERROR documented in adapter-contract.md line 492

  - Q-137: ppr.ts uses INVALID_ALPHA, INVALID_MAX_ITERATIONS,
  INVALID_CONVERGENCE_THRESHOLD, INVALID_MAX_NODES

  - Q-138: WI-731 added stats.edges_created accuracy regression test


  **Title correction applied**: Work item title corrected from "11" to "12"
  (actual count). No code changes; MCP artifact updates only. P-49: not
  applicable.
