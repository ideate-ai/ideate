id: J-071-450
type: journal_entry
phase: execute
date: 2026-04-15
cycle_created: 71
title: work-item-complete
content: >-
  WI-859 "Document new instrumentation surface" — done (verdict: rework).


  Docs updated across three files:

  - `ARCHITECTURE.md` — tool count header updated to "21 tools in 10
  categories"; new `Telemetry tools (1)` category; new `### MCP boundary
  instrumentation` subsection under Section 5 with captured columns, dispatch
  wrapper flow, fail-soft guarantee, query surface, and explicit
  cl100k_base/no-latency disclaimers.

  - `mcp/artifact-server/README.md` — tool count bumped to 21; new row for
  `ideate_get_tool_usage`; `instrumentation.ts` and `tool-usage.ts` added to
  architecture src tree.

  - `docs/platform/graphql-schema.graphql` — new "Operational Telemetry" section
  with `ToolUsageView`, `ToolUsage`, `ToolUsageAggregate`, `ToolUsageResult`,
  `ToolUsageFilterInput`, and `toolUsage` query field.


  Incremental review (sonnet): Fail with one Significant (S1) — category count
  in ARCHITECTURE.md section header said "11 categories" when Metrics→Telemetry
  was a 1-for-1 swap leaving the count at 10. Fixed to "10 categories"; all six
  acceptance criteria confirmed satisfied. Closing out at verdict `rework`.


  This closes phase PH-048 "Quality metrics rebuild". All 17 work items (WI-843
  through WI-859) complete.
