id: J-071-284
type: journal_entry
phase: review
date: 2026-04-07
cycle_created: 71
title: cycle-review-complete
content: >-
  ## Cycle 20 Capstone Review — Complete


  **Verdict**: Fail (2 critical, 5 significant, 5 minor)


  **Work items reviewed**: WI-667 through WI-673 (7 items, all passed
  incremental review; 4 required rework)


  **Test suite**: 1015 tests, 0 failures (verified via incremental reviews;
  code-reviewer hit turn budget before completing dynamic testing)


  ### Critical Findings


  - **C1** [spec-adherence] `src/adapters/local/writer.ts:474,481` — nextId()
  cycle validation uses raw field name "cycle" → VALIDATION_ERROR instead of
  INVALID_CYCLE. WI-667 missed writer.ts (scope listed local/index.ts). Remote
  adapter was correctly fixed. Corroborating test at
  nextid-cycle-validation.test.ts:232 also wrong. P-64 violation. → Q-131


  - **G1** [gap-analyst] deleteNode lacks input validation in both adapters.
  Empty id silently returns not_found instead of INVALID_NODE_ID.
  Validation-First principle violated. → Q-133


  - **G2** [gap-analyst] putEdge lacks input validation in both adapters.
  batchMutate validates edges with specific codes; putEdge bypasses all
  validation. Contract documents requirements that don't exist in code. → Q-134


  ### Significant Findings


  - **S1** [spec-adherence] adapter-contract.md:104 may still list dead
  patchNode "type" constraint despite WI-673 removal. → Q-132

  - **G3** removeEdges lacks input validation in both adapters. → Q-135

  - **G4** PARSE_ERROR not documented in adapter-contract.md. → Q-136

  - **G5** putEdge contract lists generic VALIDATION_ERROR; batchMutate uses
  specific codes for identical failures. → Q-134


  ### Domain Artifacts Written


  - D-176: vi.mock isolation — separate files for different mock configs
  (confirms P-65)

  - D-177: Equivalence test gating — LocalAdapter unconditional, RemoteAdapter
  skipIf

  - D-178: code-reviewer budget (80 turns) insufficient for deep validation
  reviews (provisional)

  - Q-131 through Q-137: Open questions from cycle 20 findings

  - Q-123: Closed (WI-672 created adapter-equivalence.test.ts)


  ### Process Note


  Code-reviewer hit 122 tool uses against 80-turn budget, returned incomplete.
  This is the second consecutive cycle with a reviewer budget exhaustion.
  Consider increasing code-reviewer budget to 120 for validation-heavy cycles.
