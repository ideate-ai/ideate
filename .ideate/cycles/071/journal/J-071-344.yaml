id: J-071-344
type: journal_entry
phase: execute
date: 2026-04-10
cycle_created: 71
title: work-item-complete
content: >-
  ## WI-749: Spike — investigate domain_index NodeType


  **Question**: Was `domain_index` planned but never implemented, or is it
  present throughout the stack?


  **Finding**: Fully implemented. Present in:

  - `adapter.ts`: `ALL_NODE_TYPES` (28 members), `NodeType` union,
  `YAML_DIR_MAP`, `TYPE_TO_PREFIX`

  - `LocalAdapter` reader: handled in `getNode`, `getAllNodes`, `traverse`,
  query filtering

  - `RemoteAdapter`: mapped through GraphQL layer


  The stale count assertion in `always-include-types-validation.test.ts`
  (`.toBe(27)`) was the real artifact of the issue — not a missing
  implementation, but a test that wasn't updated when `domain_index` was added.


  **Artifacts written**:

  - D-190: Decision record documenting domain_index is fully implemented,
  non-issue

  - Q-145: Closed (status → resolved, references D-190)
