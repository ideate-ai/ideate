id: J-071-258
type: journal_entry
phase: execute
date: 2026-04-06
cycle_created: 71
title: work-item-complete
content: >-
  ## WI-652 Complete: Transaction Error Handling


  **Status**: Completed


  ### Changes Made


  1. **writer.ts** (`src/adapters/local/writer.ts:868-879`)
     - Added try-catch with `ValidationError` throw for `deleteNode` transaction failures
     - Error includes `TRANSACTION_FAILED` code and operation context (`{ operation: "deleteNode", id }`)
     - Aligns with existing patterns in `putNode`, `patchNode`, and `batchMutate`

  2. **client.ts** (`src/adapters/remote/client.ts:96-101`)
     - Added `TRANSACTION_FAILED` error code mapping to `mapGraphQLError()`
     - Ensures RemoteAdapter can handle transaction failures from server

  3. **write-transaction.test.ts** (`src/__tests__/write-transaction.test.ts`)
     - Added test: "throws ValidationError with code TRANSACTION_FAILED on deleteNode failure"
     - Test patches `db.transaction()` to simulate failure and verifies error code and details

  ### Verification


  - All 22 write-transaction tests pass

  - All 32 local-adapter-validation tests pass

  - Adapter equivalence maintained between LocalAdapter and RemoteAdapter


  ### Acceptance Criteria Status


  | Criterion | Status |

  |-----------|--------|

  | All console.error calls replaced with ValidationError | ✅ Already done +
  added deleteNode |

  | Error messages include operation context | ✅ All operations include context
  |

  | Error code is TRANSACTION_FAILED | ✅ |

  | Existing tests pass | ✅ 54 tests pass |

  | New test verifies ValidationError | ✅ deleteNode test added |
