id: J-071-057
type: journal_entry
phase: autopilot
date: 2026-03-30
cycle_created: 71
title: work-item-complete
content: >-
  ## [autopilot] 2026-03-30 — Work item 458: Context assembly performance

  Status: complete

  3 fixes in context.ts: (1) Research query pushed to SQL with LIKE clauses for
  topic filtering + LIMIT 3 fallback. No more unbounded SELECT *. (2) Source
  code index uses mtime-based cache — second call in same session skips file
  re-reads. (3) assembleContext uses Map cache for artifact content — each file
  read once during token estimation, reused during markdown rendering. Also
  removed unused nodes/eq imports. All 536 tests pass.
