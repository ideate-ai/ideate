id: J-071-451
type: journal_entry
phase: execute
date: 2026-04-15
cycle_created: 71
title: phase-complete
content: >-
  Phase PH-048 "Quality metrics rebuild" — all work items done.


  ## Scope delivered


  YAGNI-driven lean rebuild of the quality metrics surface:


  **Remove (WI-843 through WI-852 + WI-848 schema cleanup)** — deleted the
  failed `ideate_emit_metric` / `ideate_get_metrics` experiment:

  - Stripped agent-side `ideate_emit_metric` calls from skill markdown (WI-843)
  and autopilot skill files (WI-844).

  - Removed tool registrations and dispatch (WI-845).

  - Removed `metricsEvents` Drizzle table and schema registry (WI-846).

  - Removed `metrics_event` indexer case (WI-847).

  - Removed adapter metrics method, impl, and db-helpers (WI-848).

  - Removed remote adapter metrics stub (WI-849).

  - Deleted metrics YAML dir and four legacy report scripts (WI-850).

  - Deleted metrics tests and cleaned 3 test files (WI-851).

  - Removed metrics docs (WI-852).


  **Add (WI-853 through WI-858)** — replaced with narrow MCP-boundary
  instrumentation:

  - Designed `tool_usage` SQLite schema, Drizzle definition, and types —
  standalone table per P-33 / D-210 (WI-853).

  - Added `getToolUsage` to local adapter (WI-854) and stub to remote adapter
  (WI-855).

  - `instrumentToolDispatch` fail-soft wrapper + `countTokens` cl100k_base
  helper (WI-856).

  - `ideate_get_tool_usage` MCP tool with aggregate/detail/both views and filter
  fields (WI-857).

  - `scripts/report-tool-usage.sh` replacement for the deleted legacy report
  scripts (WI-858).


  **Document (WI-859)** — ARCHITECTURE.md, README.md, graphql-schema.graphql
  updated with new subsection, tool count adjustment (21 tools in 10 categories;
  net −1 from pre-phase baseline of 22), tool table row, GraphQL ToolUsage type
  family, and explicit cl100k_base + no-latency disclaimers.


  ## Outcome


  - MCP tool surface: 20 → 21 (Metrics removed, Telemetry added — 1-for-1
  category swap).

  - Instrumentation: agent-side emission removed (D-211); boundary emission via
  dispatch wrapper (P-33).

  - Report scripts: four deleted, one replacement covering the same reporting
  need.

  - All incremental reviews passed (rework on WI-859 for a category-count
  correction).


  ## Suggested next step


  Run `/ideate:review` to run the capstone review for this phase and check phase
  convergence against project success criteria.
