id: J-071-455
type: journal_entry
phase: execute
date: 2026-04-16
cycle_created: 71
title: wave-complete
content: >-
  PH-048 wave 2 complete (autopilot cycle 1). All three items done after one
  rework round each.


  **WI-863**: Contract tests added for getToolUsage across views and filter
  round-trip. ValidatingAdapter.getToolUsage now validates
  id/tool_name/timestamp shape; negative test asserts it throws on malformed
  rows. Annotated that limit is handler-level (not adapter) — acceptable spec
  interpretation. 107/107 pass.


  **WI-864**: Fixed directional error in truncation comment (was "oldest
  omitted" — actual behavior: newest omitted under timestamp ASC ordering).
  Added truncation test with row-identity assertions that would catch a DESC
  regression. 1471/1471 pass.


  **WI-866**: Moved --cycle out of zero-reject loop (cycle 0 is legitimate).
  Error messages improved ("must be >=1" vs misleading "omit for default"). All
  manual test cases pass.


  **Rework pattern observation for curator**: All three wave-2 items had Fail on
  first review. Root causes: (a) WI-863 spec ambiguity on "limit filter"
  (interface doesn't have it), (b) WI-864 comment direction not self-checked by
  test, (c) WI-866 over-eager validation loop capturing a legitimately valid
  value. Curator may want to add a domain policy about writing tests that would
  catch the invariant being claimed (e.g., direction-aware assertions, not just
  count assertions).


  PH-048 execution complete. Proceeding to capstone review.
