id: J-071-309
type: journal_entry
phase: execute
date: 2026-04-08
cycle_created: 71
title: work-item-complete
content: >
  ## WI-718 — Add addressed_by edge derivation from finding verdict + work_item
  fields

  Status: complete with rework

  Rework: 2 minor findings fixed from incremental review.

  - M1: Added `explicitAddressedBy` guard to skip derivation emit when
  yaml_field path already emitted the same edge — prevents edgesCreated
  overcount on double-fire scenario

  - M2: Added test case for double-fire scenario (yaml_field + derivation both
  point to same WI) — confirms exactly 1 edge emitted


  Implementation: added derivation block at end of extractEdges() in indexer.ts.
  Guard regex /^WI-\d+$/ prevents free-text work_item values from producing
  dangling edges. 5 new tests added (4 derivation cases + 1 double-fire dedup).
  1137 tests pass.


  ## WI-724 — Fix non-canonical 'completed' status values in work item YAML
  files

  Status: complete

  Updated 11 work items (WI-653–WI-660, WI-683, WI-684, WI-685) from
  status='completed' to 'done' via ideate_update_work_items. 0 rows with
  completed status remain. Workspace status 'completed' bucket eliminated.
