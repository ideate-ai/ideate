id: J-071-262
type: journal_entry
phase: execute
date: 2026-04-06
cycle_created: 71
title: work-item-complete
content: >-
  ## WI-656 Complete: Align fetchCurrentCycle Error Recovery


  **Status**: Already implemented — verified complete


  ### Implementation Status


  **RemoteAdapter** (`src/adapters/remote/index.ts:418-464`):

  - JSON parse errors throw `StorageAdapterError` with `PARSE_ERROR` code

  - GraphQL/network errors throw `ValidationError` with context

  - Missing/empty domain_index returns `null` (no cycle data yet)

  - No silent fallback to cycle 1 — all errors are thrown explicitly


  ### Error Handling Behavior


  | Scenario | Behavior |

  |----------|----------|

  | Malformed JSON in domain_index | Throws `StorageAdapterError` (PARSE_ERROR)
  |

  | GraphQL/network error | Throws `ValidationError` with error context |

  | Missing domain_index artifact | Returns `null` (no error) |

  | Empty domain_index content | Returns `null` (no error) |


  ### Test Coverage


  All 18 remote JSON parse tests pass:

  - ✓ JSON.parse errors throw StorageAdapterError with PARSE_ERROR code

  - ✓ GraphQL errors throw ValidationError with context

  - ✓ No silent fallback to cycle 1 on errors
