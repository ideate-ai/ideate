id: J-071-334
type: journal_entry
phase: execute
date: 2026-04-09
cycle_created: 71
title: work-item-complete
content: >-
  ## WI-744: Fix stale handler names in architecture doc file registry


  Corrected two stale handler names in `.ideate/plan/architecture.yaml` file
  registry via `ideate_write_artifact` (MCP only, no direct file edits per
  P-37):


  - `handleGetWorkItemContext` → `handleGetArtifactContext` (context.ts entry) —
  matches exported function at `context.ts:216`

  - `handleGetProjectStatus` → `handleGetWorkspaceStatus` (analysis.ts entry) —
  matches exported function at `analysis.ts:650`


  These were stale since WI-740 (cycle 20) updated tool names and counts but
  missed the handler names in the file registry section. No source code changes.
  No git commit required (MCP-only artifact update).
