id: J-071-313
type: journal_entry
phase: execute
date: 2026-04-09
cycle_created: 71
title: work-item-complete
content: >-
  ## WI-726: Add delete-before-insert to deriveJournalEntryCycleEdges

  Status: complete with rework

  Rework: 2 significant findings fixed from incremental review.

  - S1: Worker modified schema.ts out of scope (WI-725 owns that file) —
  reverted before commit.

  - S2: Idempotency test called rebuildIndex twice (which wipes all edges in
  Phase 1, masking the targeted delete) — rewrote test to call
  deriveJournalEntryCycleEdges directly on a upsert-seeded DB.

  Also: exported deriveJournalEntryCycleEdges for direct testability; moved
  JOURNAL_ID_CYCLE regex to module scope. Stash pop after merge produced
  conflicts in journalEntryYaml helper signature — resolved by merging into
  single function with default title param. 1159 tests pass.
