id: J-071-150
type: journal_entry
phase: execute
date: 2026-04-02
cycle_created: 71
title: work-item-complete
content: >-
  ## [execute] 2026-04-02 — Work item 556: Config extension for backend
  selection

  Status: complete

  Added backend field ('local'|'remote') to config.ts with
  DEFAULT_BACKEND='local'. Added remote connection fields (url, org_id,
  auth_token). server.ts selectAdapter() routes by config.backend, throws clear
  error for 'remote'. CONFIG_SCHEMA_VERSION bumped 3→4 with migration in
  migrations.ts. 583/583 tests pass. All 5 criteria satisfied.
