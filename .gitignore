# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg

# MCP artifact server — dist is committed so it works as a plugin without a build step
# node_modules is NOT committed — start.sh installs deps on first run
!mcp/artifact-server/dist/
!mcp/artifact-server/dist/**
node_modules/

# Virtual environments
.venv/
venv/
env/

# pytest
.pytest_cache/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local
specs/metrics.jsonl
